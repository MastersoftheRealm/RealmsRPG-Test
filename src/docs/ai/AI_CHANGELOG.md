# AI Change Log

Append-only log. Add an entry per PR/merge. **Recent entries (last ~15) suffice for context; older entries are in [`archive/AI_CHANGELOG_ARCHIVE.md`](archive/AI_CHANGELOG_ARCHIVE.md).**

> Entries before 2026-04-01 are in [`archive/AI_CHANGELOG_ARCHIVE.md`](archive/AI_CHANGELOG_ARCHIVE.md).

- 2026-06-27 | agent | Session: TASK-385 authenticated visual + a11y baselines | files: provision-e2e-baseline.js, e2e-seed-manifest.json, auth.setup.ts, auth-fixture.ts, auth-screenshots.pw.ts, auth-a11y.pw.ts, auth-a11y-baseline.json, playwright.auth.config.ts, ui-verify.yml, package.json, AI_TASK_QUEUE, DEVELOPER_TASK_QUEUE, BUILD_VALIDATION, UI_UNIFICATION_PLAN, VISUAL_STATE_AUDIT | Summary: Full auth safety net — storageState login, deterministic seed (fixed character/campaign UUIDs), 10 visual snapshots × light/dark, ratcheted auth a11y. `npm run e2e:provision` creates test user in Supabase. CI optional step when E2E secrets set. **TASK-385 ✅**.

- 2026-06-27 | agent | Session: UI unification sanity closeout | files: styleguide main-content, text-danger-fg stragglers, species-trait-card/item-card a11y, auth-fixture + auth-screenshots.pw.ts, package.json, UI_UNIFICATION_PLAN, VSEA, AI_TASK_QUEUE TASK-385 partial | Summary: Final spot-check fixes + TASK-385 scaffold (skips without E2E secrets). Execution order doc synced. Verify green.

- 2026-06-27 | agent | Session: UI unification final closeout — TASK-384 a11y zero baseline | files: toast, tab-navigation, input/select/textarea/checkbox, privacy, styleguide, point-status, globals tab-nav-active, a11y-baseline.json, AI_TASK_QUEUE, UI_UNIFICATION_PLAN, VISUAL_STATE_AUDIT | Summary: Cleared all axe baseline allowances (30 routes × 2 themes). Fixed global toast ARIA, tab disabled semantics, form error contrast, privacy link distinguishability, styleguide token sampling, PointStatus on tinted surfaces. **UI unification Phases 0–5 ✅**; TASK-385 remains human-gated.

- 2026-06-27 | agent | Session: Phase 5.6 — gap closure (100% Phase 5) | files: globals.css z-popover/skip-link + motion keyframes, toast/motion.ts, tooltip, layout skip-link, auth/about motion, focus-ring sweep (~20 files), home-page layout-shell-wide, CombatantCard touch, admin/core-rules/tooltips + skills TableScroll, UI_UNIFICATION_PLAN.md, DESIGN_SYSTEM.md, FEATURE_INDEX.md | Summary: Closed all Phase 5 audit gaps — no deferred focus rings, z-index stragglers, or table wrappers. Phase 5 ✅ complete.

- 2026-06-27 | agent | Session: Phase 5.5 — breakpoints + capitalization / Phase 5 complete | files: globals.css layout-shell-wide, header/footer/home-page, formatColumnKeyLabel, grid-list-row, species-creator, UI_UNIFICATION_PLAN.md, DESIGN_SYSTEM.md | Summary: Unified marketing chrome on `--container-wide` + shared gutters. Central column-key labels for GridListRow; species-creator trait rows fixed. Phase 5 ✅.

- 2026-06-27 | agent | Session: Phase 5.4 — touch target audit | files: globals.css touch-target utilities, tab triggers, CombatantCard, roll-button, chip, MOBILE_UX.md, UI_UNIFICATION_PLAN.md, styleguide baselines | Summary: Added `.touch-target` / `.touch-target-md-compact` utilities; bumped tab triggers + encounter card controls to 44px below `md`. ValueStepper/creator tab bar already compliant. Verify green.

- 2026-06-27 | agent | Session: Phase 5.3 — focus ring unification | files: button, icon-button, input/select/textarea, checkbox, tooltip, chip, roll-button, globals.css (input-field, search, tabs, focus-ring), UI_UNIFICATION_PLAN.md, DESIGN_SYSTEM.md | Summary: Standardized primitive focus rings on `primary-outline-border`; keyboard controls use `focus-visible`; form controls use `:focus:`; error states use `danger-border`. Verify green.

- 2026-06-27 | agent | Session: Phase 5.2 — motion token migration | files: card, toast, expandable-chip, roll-log, sheet-action-toolbar/header, shared toggles/cards/chips, character-card, globals.css list-item/hover-lift, UI_UNIFICATION_PLAN.md, DESIGN_SYSTEM.md | Summary: Migrated app primitives + shared surfaces from hardcoded `duration-200/300` to semantic `duration-base`/`duration-slow`/`ease-standard`. Auth/about intentionally unchanged. CSS `@apply` cannot use custom duration utilities — use CSS vars there. Verify green.

- 2026-06-27 | agent | Session: Phase 5.1 — responsive/a11y foundations | files: `globals.css`, `table-scroll.tsx`, quick-armaments, archetype-section, item-creator, admin/users, button/chip/modal/toast/spinner, header, roll-log, sheet-action-toolbar, modals alerts, creator footers, DESIGN_SYSTEM.md, UI_UNIFICATION_PLAN.md | Summary: Added z-index ladder + global `prefers-reduced-motion`. New `<TableScroll>` for horizontal table overflow on narrow viewports. Migrated overlay/toast/floating z-index to semantic utilities; Button/Chip/Modal backdrop use `duration-base ease-standard`.

- 2026-06-27 | agent | Session: Phase 4.10 — gap closure / Phase 4 complete | files: status-surface-classes.ts, ancestry/feats/finalize/equipment/archetype steps, creator-tab-bar, CombatantCard (prior), chip/globals borders, modals, admin, crafting, creature-creator, item-creator, resources, UI_UNIFICATION_PLAN.md | Summary: Closed all session-identified Phase 4 gaps. Added shared statusPanel helper; migrated creator validation banners and remaining numbered ramp maps; removed modal hex overrides; unified chip border tokens. Phase 4 ✅. Verify green.

- 2026-06-27 | agent | Session: Phase 4.7–4.9 — encounters in-session + rarity table + slider hex | files: CombatantCard, SkillEncounterView, item-creator/page, crafting/page, recovery-modal, encounters/page, UI_UNIFICATION_PLAN.md | Summary: Migrated CombatantCard and SkillEncounterView to semantic combatant/health/energy/status tokens. Item creator RARITY_REFERENCE uses Chip + rarityChipVariant. Recovery slider uses CSS vars. Verify green.

- 2026-06-27 | agent | Session: Phase 4.6 — chip surface backgrounds | files: `chip.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Migrated list/listCost/listWarning/listSuccess/tp/proficiency chip variants off numbered ramps + dark: pairs to theme-aware semantic tokens. Verify green.

- 2026-06-27 | agent | Session: Phase 4.5 — power/martial + encounter domain colors | files: archetype-section, edit-archetype-modal, archetype-step, recovery-modal, grid-list-row, sheet-header, encounters/page, CombatEncounterView, CombatantCard, globals.css, chip.tsx, tab-summary-section, part-chip-variant.ts, DESIGN_SYSTEM.md, UI_UNIFICATION_PLAN.md | Summary: Cleared remaining violet/red numbered ramp + dark: pairs from power/martial UI and encounter domain maps. Semantic tokens: accent-fg/border, info-border. rarityAscended chip unified. DESIGN_SYSTEM PageContainer table synced. Verify green.

- 2026-06-27 | agent | Session: Phase 4.4 — shared power violet cleanup | files: `powered-martial-slider.tsx`, `innate-toggle.tsx`, `sheet-action-toolbar.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Migrated three shared/leaf power UI surfaces off violet numbered ramps to `text-power-fg`, `bg-power-light`, `border-power-border`.

- 2026-06-27 | agent | Session: Phase 4.3 — TabSummarySection semantic tokens | files: `tab-summary-section.tsx`, `globals.css`, `styleguide/page.tsx`, `UI_UNIFICATION_PLAN.md`, `VISUAL_STATE_AUDIT.md` | Summary: Migrated all five TabSummarySection variants off violet/success/warning/info numbered ramps + `dark:` pairs onto theme-aware domain tokens. Power highlight → `text-power-fg`. Dark domain border overrides. Styleguide shows all variants. Verify green.

- 2026-06-27 | agent | Session: Phase 3/4 review — dead maps removed, mapper unified | files: `creator-constants.ts`, `part-chip-variant.ts`, `power-part-card.tsx`, deleted `part-category.ts`, `styleguide/page.tsx`, `chip.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Review found `CATEGORY_COLORS`/`RARITY_COLORS` were dead (Phase 2.2 already migrated parts to chips). Removed both maps; merged codex label fuzzy matching into `partChipVariant()`; added rarity swatches to styleguide. Verify green.

- 2026-06-27 | agent | Session: Phase 3 close + Phase 4.1 rarity chips | files: `styleguide/page.tsx`, `page-container.tsx`, `globals.css`, `chip.tsx`, `rarity-chip-variant.ts`, `creator-summary-panel.tsx`, `item-creator/page.tsx`, `creator-constants.ts`, styleguide baselines, `UI_UNIFICATION_PLAN.md` | Summary: **Phase 3 ✅** — styleguide uses PageContainer/PageHeader; container sizes use CSS var ladder; removed dead `.page-container` CSS. **Phase 4.1** — rarity badge → canonical chip variants. Verify green.

- 2026-06-27 | agent | Session: Phase 3 increment 3 — my-account padding + error pages | files: `my-account/page.tsx`, `(main)/error.tsx`, `error.tsx`, `not-found.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Removed duplicate my-account outer wrapper; enabled default `PageContainer` padding. Migrated error and not-found boundaries to `PageHeader`. Verify green.

- 2026-06-27 | agent | Session: Phase 3 increment 2 — layout + encounter headers | files: `page-container.tsx`, `page-header.tsx`, `EncounterPageHeader.tsx`, combat/skill/mixed encounter pages, `characters/[id]/page.tsx`, campaign character view, creator page wrappers (power/technique/item/empowered/creature), `skills-allocation-page.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Added `PageContainer` `tool` (1600px). Unified encounter headers via shared component with editable title. Extended `PageHeader` (ReactNode title/description, `onTitleClick`, `font-display`). Removed duplicate padding wrappers on creators. Character sheet + campaign view on `size="tool"`. Verify green.

- 2026-06-27 | agent | Session: Gap closure — primary codemod, VSEA-002, Phase 3 PageHeader increment 1 | files: `scripts/migrate-primary-tokens.mjs`, ~110 TSX files, `styleguide/page.tsx`, `my-account/page.tsx`, `characters/new/page.tsx`, `campaigns/[id]/page.tsx`, `encounters/[id]/mixed/page.tsx`, `sheet-action-toolbar.tsx`, `recovery-modal.tsx`, `edit-section-toggle.tsx`, styleguide visual baselines, `UI_UNIFICATION_PLAN.md`, `VISUAL_STATE_AUDIT.md` | Summary: Closed all documented Phase 2 carryover gaps. Codemod + manual pass migrated numbered `primary-[0-9]` class usage to semantic tokens across app/components. Added styleguide Interactive State Matrix (VSEA-002 ✅). Migrated four routes to `PageHeader`. Fixed mixed encounter header JSX. Full `npm run verify` green.

- 2026-06-27 | agent | Session: Phase 2.3 complete — final inline panels → `<Card>` | files: roll-log, sheet-header, dice-roller, power/technique/empowered/item creators, characters/new, ancestry/equipment/archetype steps, species-modal, creature-stat-block, `UI_UNIFICATION_PLAN.md` | Summary: Migrated last 15 inline `bg-surface rounded-xl` panels. Grep-verified **zero** remain in `src/`. Phase 2.3 ✅; **Phase 2 fully complete**. Verify green.

- 2026-06-27 | agent | Session: Phase 2.3d — encounters, account, creature-creator → `<Card>` | files: `my-account/page.tsx`, `SkillEncounterView.tsx`, `CombatEncounterView.tsx`, `CombatantCard.tsx`, `creature-creator/page.tsx`, `resources/page.tsx`, `creator-summary-panel.tsx`, `skills-allocation-page.tsx`, `resources` visual baselines, `UI_UNIFICATION_PLAN.md` | Summary: Migrated account settings sections, full encounter UI panels + combatant/participant cards, creature-creator stat blocks, creator summary sidebar, skills allocation tables, and resources page to `<Card>`. Updated resources mobile visual baselines (+2px from Card border). Verify green.

- 2026-06-27 | agent | Session: Phase 2.3 batch — character sheet + Card CVA tokens | files: `card.tsx`, `hub-list-row.tsx`, `abilities-section.tsx`, `archetype-section.tsx`, `skills-section.tsx`, `library-section.tsx`, `sheet-header.tsx`, `collapsible-section.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Updated `cardVariants` selected/interactive hovers to semantic primary tokens. Migrated all five character-sheet section shells + hub list rows + creator collapsible sections to `<Card>`. Verify green.

- 2026-06-27 | agent | Session: Phase 2.3 increment — inline panels → `<Card>` | files: `rules/page.tsx`, `crafting/[id]/page.tsx`, `dice-roller.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Migrated rules iframe wrapper, crafting completed-outcome section, and dice-roller shell off raw `bg-surface rounded-xl` divs to `<Card>`. Verify green.

- 2026-06-27 | agent | Session: Phase 2.5b batches 2–3 — domain primary `dark:` cleanup | files: character-creator steps, character-sheet (dice-roller, feats-tab, archetype-section, roll-log, sheet-header, recovery-modal, edit-* modals), shared (onboarding-tour, add-combatant-modal, skill-row, point-status, tab-summary-section, edit-section-toggle, confirm-action-modal, powered-martial-slider, roll-button), codex tabs, encounters/CombatantCard, creature-creator, species-creator, modal.tsx, `UI_UNIFICATION_PLAN.md` | Summary: Migrated character creator/sheet and high-traffic shared surfaces off `dark:text-primary-*` / `dark:bg-primary-*` / `dark:border-primary-*` pairs onto theme-aware semantic tokens (`primary-fg`, `primary-link-fg`, `primary-subtle-*`, `primary-button`, `primary-outline-*`). **Zero** primary `dark:` pairs remain in `src/`. Intentionally kept: ancestry-step domain color maps (amber/blue/red), roll-button unproficient variant surface `dark:`, auth inline loading copy, numbered ramp on solid FABs/admin links. Full `npm run verify` green.

- 2026-06-27 | agent | Session: Phase 2.5b batch 1 — marketing + shared selection | files: `globals.css`, `home-page.tsx`, `about/page.tsx`, `rules/page.tsx`, `crafting/page.tsx`, `encounters/page.tsx`, `footer.tsx`, `theme-toggle.tsx`, `segmented-control.tsx`, `list-header.tsx`, `skill-row.tsx`, `markdown-lite.tsx`, `check-contrast.mjs`, `styleguide/page.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Added `primary-subtle-*` tokens. Migrated public/marketing pages and high-traffic shared components off `dark:text-primary-*` pairs. SegmentedControl selected → `bg-primary-button`. Verify green.

- 2026-06-27 | agent | Session: Phase 2.4 complete + Phase 2.5 primary tokens | files: `globals.css`, `button.tsx`, `chip.tsx`, `header.tsx`, `tooltip.tsx`, `feats-step.tsx`, `check-contrast.mjs`, `styleguide/page.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: **2.4 ✅** feats-step empties → `EmptyState`. **2.5 started:** theme-aware primary semantic tokens (`primary-fg`, `primary-link-fg`, `primary-chip-*`, `danger-button`, etc.); migrated Button link/outline/danger, Chip primary, full header nav, tooltip trigger. Contrast pairs added. Domain surfaces still use numbered ramp + `dark:` (documented 2.5b).

- 2026-06-27 | agent | Session: Phase 2.4 batch 2 — admin, encounters, roll-log, powers-step | files: `roll-log.tsx`, `campaigns/[id]/page.tsx`, `CombatEncounterView.tsx`, `SkillEncounterView.tsx`, `admin/users|roles|tooltips|changelogs|core-rules`, `CodexSpreadsheetView.tsx`, `official-entity-list.tsx`, `powers-step.tsx`, `dice-roller.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Migrated remaining high-traffic loading/empty stragglers. Encounter empties use `<Card>` + `<EmptyState>`. Admin pages off raw Spinner divs. powers-step dashed placeholders → `EmptyState`. Verify green.

- 2026-06-27 | agent | Session: Phase 2.4 — codex + library + modal empty/loading | files: `Codex*Tab.tsx` (6), `Library*Tab.tsx` (5), `characters/page.tsx`, `add-combatant-modal.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Migrated all public codex tab filter-empty divs to `<EmptyState size="sm">`. Library search-empty → `ListEmptyState`. Characters search-empty + add-combatant loading/empty → `LoadingState`/`EmptyState`. Verify green.

- 2026-06-27 | agent | Session: Phase 2.4 start — loading/empty standardization | files: `home-page.tsx`, `item-creator/page.tsx`, `power-creator/page.tsx`, `empowered-technique-creator/page.tsx`, `CodexPartsTab.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Replaced raw `Loading...` Suspense fallbacks with `<LoadingState>` on home + three creator pages. Codex parts empty → `<EmptyState>`. More codex tabs and inline empty divs remain for follow-up 2.4 passes.

- 2026-06-27 | agent | Session: Phase 2.2 complete + Phase 2.3 Card CVA start | files: `chip.tsx`, `powers-step.tsx`, `card.tsx`, `selection-card.tsx`, `archetype-step.tsx`, `species-step.tsx`, `edit-archetype-modal.tsx`, `globals.css`, `styleguide/page.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: **2.2 done:** added `tp` Chip variant; migrated powers-step TP summary. **2.3 started:** `cardVariants` CVA on `<Card>`; new `SelectionCard`/`SelectionCardSurface`; migrated all `.selection-card` usages; removed dead `.card`/`.interactive-card`/`.selection-card` CSS. Inline card panels (encounters, my-account, etc.) deferred to incremental 2.3 passes.

- 2026-06-27 | agent | Session: Phase 2.2 partial — PartChip + ExpandableChip + changelogs | files: `part-chip.tsx`, `expandable-chip.tsx`, `lib/chip/part-chip-variant.ts`, `admin/changelogs/page.tsx`, `UI_UNIFICATION_PLAN.md` | Summary: Migrated PartChip and ExpandableChip off parallel `categoryStyles` maps onto `chipVariants` via `partChipVariant()`. Fixed PartChip using non-existent `bg-category-*-bg` tokens (now uses canonical category variants). Admin changelogs operation badges → `<Chip>`.

- 2026-06-27 | agent | Session: Phase 2.2 partial — GridListRow chip consolidation | files: `chip.tsx`, `grid-list-row.tsx`, `expandable-grid-list-chip.tsx`, `grid-list-row-types.ts`, `lib/chip/grid-list-chip-variant.ts`, `styleguide/page.tsx`, `UI_UNIFICATION_PLAN.md`, `VISUAL_STATE_AUDIT.md` | Summary: Phase 2.2 increment 1 — removed parallel `CHIP_STYLES` map from GridListRow; added canonical `list`/`listCost`/`listWarning`/`listSuccess` variants to `<Chip>` CVA; extracted `ExpandableGridListChip` + shared `ChipData` types. PartChip and ad-hoc inline chips remain for 2.2 follow-up.

- 2026-06-27 | agent | Session: Phase 1.6 — light-only status ramp cleanup + pre-Phase-2 review | files: `archetype-section.tsx`, `roll-log.tsx`, `species-trait-card.tsx`, `value-stepper.tsx`, `species-modal.tsx`, library tabs, `campaigns/page.tsx`, `CodexPartsTab.tsx`, `grid-list-row.tsx`, `dice-roller.tsx`, `level-up-modal.tsx`, `power-creator/page.tsx`, `species-creator/page.tsx`, `skill-row.tsx`, `UI_UNIFICATION_PLAN.md`, `VISUAL_STATE_AUDIT.md` | Summary: Pre-Phase-2 review found ~20 files still using light-only status ramps (`text-*-700/800`) without `*-fg`. Migrated all TSX stragglers; fixed VSEA-001 (`GridListRow` `warning` chip now uses warning tokens). Added Phase 1.4 + 1.6 rows to plan; clarified Phase 1 completion criteria. Zero `dark:` status pairs repo-wide; only legacy `.alert-*` utilities in `globals.css` remain on ramps (Phase 2).

- 2026-06-27 | agent | Session: Phase 1.4 — feature-layer fg token migration | files: `src/components/character-sheet/*`, `src/components/character-creator/*`, `src/app/**`, `src/components/encounters/CombatantCard.tsx`, `src/components/layout/header.tsx`, `src/components/creator/archetype-selector.tsx`, `scripts/migrate-fg-tokens.mjs`, `UI_UNIFICATION_PLAN.md` | Summary: Completed Phase 1.4 — migrated all remaining status foreground pairs in character-sheet (12 files), character-creator (8 files), app routes (26 files via codemod), and stragglers (CombatantCard, header, archetype-selector). Added reusable `scripts/migrate-fg-tokens.mjs`. Repo-wide grep: **0** remaining `text-*-N dark:text-*` status patterns in `src/`. Full `npm run verify` green (no visual baseline changes — token swaps only).

- 2026-06-27 | agent | Session: VSEA integrated + Phase 1.3 shared components | files: `src/docs/ai/VISUAL_STATE_AUDIT.md`, `UI_UNIFICATION_PLAN.md`, `AGENT_GUIDE.md`, `AGENTS.md`, `src/components/shared/*` (11 files) | Summary: Added **Visual State Exploration Audit** as cross-cutting process — full interaction checklist, coverage tracker, retroactive queue for Phase 1.1–1.2, findings log (`VSEA-###`), automation roadmap. Gate: explore+log all meaningful states before refactoring a surface. **Phase 1.3:** migrated remaining `src/components/shared/` leaf components off `text-*-700 dark:text-*-400` pairs to theme-aware `text-*-fg` (edit-section-toggle, selection/equip toggles, item-card, creature-stat-block, species-trait-card, skill-row, part-chip, grid-list-row, value-stepper, add-skill-modal). Logged VSEA-001 (`GridListRow` `warning` chip uses danger colors — fix in Phase 2) and VSEA-002 (styleguide needs interactive state matrix). Full `npm run verify` green.

- 2026-06-27 | agent | Session: UI unification Phase 1.1 — UI primitives → semantic *-fg tokens | files: `src/components/ui/button.tsx`, `chip.tsx`, `alert.tsx`, `toast.tsx`, `src/app/dev/styleguide/page.tsx`, `tests/visual/screenshots.pw.ts-snapshots/styleguide-*`, `src/docs/ai/UI_UNIFICATION_PLAN.md` | Summary: Sanity-checked Phase 0 (contrast 56/56, compat shim intact, zero external legacy `var()` consumers, raw-color backlog at 0 files) then started Phase 1 leaf-first on UI primitives. Replaced ad-hoc `text-*-700 dark:text-*-300` pairs with theme-aware semantic tokens (`text-success-fg`, `text-danger-fg`, `text-warning-fg`, `text-info-fg`, `text-power-fg`, `text-martial-fg`) in Chip status/deprecated archetype variants, Alert, and Toast. Button `primary` now uses `bg-primary-button` (drops redundant `dark:bg-primary-100 dark:text-white`; hover preserved via `hover:bg-primary-700 dark:hover:bg-primary-50`). Chip `primary` left on ramp+dark (no matching semantic token yet — avoids light-mode shade shift). Added "Semantic Foreground (theme-aware)" swatch section to `/dev/styleguide`. Visual: only styleguide baselines updated (new section = taller page); other 48 screenshots unchanged. Full `npm run verify` green.

- 2026-06-27 | agent | Session: UI unification Phase 0.2–0.5 — namespace collapse, ladders, dead-CSS prune, design-system docs | files: `src/app/globals.css`, `src/docs/DESIGN_SYSTEM.md`, `src/docs/ai/UI_UNIFICATION_PLAN.md` | Summary: Completed Phase 0 of the UI unification roadmap. **0.2 (collapse dual namespaces):** legacy `:root` vars now alias `var(--color-*)` (single source of truth); removed duplicated `.dark` legacy-hex lines; `body` points at canonical tokens. **0.3 (standards ladders):** added additive semantic `@theme` ladders — radius (`--radius-control/-card/-pill`), elevation (`--shadow-card/-raised/-overlay`), motion (`--duration-fast/-base/-slow`, `--ease-standard`), containers (`--container-narrow/-standard/-wide/-full-tool`); values = dominant existing usage, no visual change. **0.4 (prune dead CSS):** grep-verified 0 className usage across `src/**/*.{tsx,ts}` (with quote/space boundaries to avoid substring/comment false-positives) before deleting — removed legacy gradient buttons (`.btn-primary/-secondary/-danger/-success/-outline/-ghost/-utility/-icon`), `.page-title`, `.section-title`, `.container-main`, `.page-container-wide/-narrow`, `.page-header*` (the `PageHeader` component uses Tailwind utilities, not these classes), all `.chip`/`.chip-*` (app uses the CVA `<Chip>` component), `.trait-chip*`, `.creator-tab*`, `.loading-spinner*`, `.divider-vertical`, `.list-container-compact`, `.modal-header-gradient`. **Kept** the live classes: `.btn-solid`, `.btn-outline-clean`, `.btn-stepper*`, `.card`, `.interactive-card`, `.selection-card*`, `.tab-nav*`, `.tab-pill*`, `.skeleton*`, `.page-container`, `.font-display`, plus the `--color-accent-chip` token (still used by `<Chip variant="accent">`). **0.5 (docs):** rewrote `DESIGN_SYSTEM.md` with a Token Architecture section (two tiers + theme-aware `*-fg` tokens + standards ladders) and corrected every stale "CSS classes available" list + the inline-spinner migration example. Full `npm run verify` green end-to-end: contrast 56/56 (0 failures), lint 0 errors, visual **54 passed**, a11y **30 passed**; build exit 0; visual baselines unchanged (prune was pure dead-code removal). Phase 0 complete → next is Phase 1 (component migration onto semantic classes, then remove the compat shim).

- 2026-06-27 | agent | Session: UI unification Phase 0.1 — complete the dark theme + safety-net hardening | files: `src/app/globals.css`, `src/app/layout.tsx`, `scripts/check-contrast.mjs`, `scripts/contrast-baseline.json`, `playwright.config.ts`, `tests/visual/screenshots.pw.ts-snapshots/*`, `tests/visual/a11y-baseline.json` | Summary: Started Phase 0 of the UI unification plan and, while double-checking Phase 0a, found and fixed **two real safety-net bugs**. (1) **Contrast script bug:** `extractBlock('.dark')` matched the `@custom-variant dark (…)` mention on line 4, not the real `.dark { }` rule, so it silently compared dark≈light (all 52 "passed" identically). Fixed extractor to require a `selector` immediately followed by `{`. The corrected script then exposed **10 genuine dark-mode contrast failures** (title 2.35:1, status/power/martial/health text on dark tinted bgs, white-on-light primary). (2) **Stale-server bug:** Playwright `webServer.reuseExistingServer: true` reused a long-running server from an earlier build, so visual baselines + a11y scans were validated against the wrong build (default `#9e9eff` links on white = CSS not applied). Set `reuseExistingServer: false` so the suite always serves the build under test. **Token work (Phase 0.1):** added theme-aware semantic foreground tokens (`--color-success-fg`/`-danger-fg`/`-warning-fg`/`-info-fg`/`-power-fg`/`-martial-fg`, `--color-primary-button`) and filled every previously-light-only dark token (`title`, `health-text`, all 7 `category-*` chips, `ally/enemy/companion`, `accent-chip/-light`, `card`, `muted`, `muted-foreground`, `destructive`). Light mode unchanged (contrast ratios identical). Contrast now **56/56 pass in BOTH themes (real)**, baseline locked at 0. **Determinism fix:** self-hosted `Nova Flat` via `next/font` (was a runtime Google-Fonts CDN `<link>` with `display=swap` → layout reflow → flaky baselines); wired `--font-display` to `var(--font-nova-flat)`. Regenerated 54 visual baselines + re-recorded a11y baseline against correct fresh servers; full `npm run verify` green (contrast/lint/visual 54/visual a11y 30). Build exit 0. **Workflow hardening:** `verify` + `verify:visual:update` + `verify:a11y:update` now `npm run build` first (stale `.next` was the deeper foot-gun beyond stale running servers); added durable roadmap `src/docs/ai/UI_UNIFICATION_PLAN.md` (the plan was a Cursor plan-mode artifact never persisted — now source-controlled and referenced from `AGENT_GUIDE.md` + `AGENTS.md` source-of-truth map); documented token architecture (theme-aware `*-fg` tokens) + hard-won gotchas (build-first, `reuseExistingServer:false`, self-hosted fonts, "verify your verifier"; `#9e9eff`-links-on-white = stale/unstyled render tell). Remaining Phase 0: dual-namespace collapse (legacy `:root` → `var(--color-*)` aliases), standard ladders (radius/elevation/motion/focus/containers) as tokens, prune verified-dead CSS, rewrite `DESIGN_SYSTEM.md`.

- 2026-06-26 | agent | Session: UI unification Phase 0a — automated verification safety net (TASK-383) | files: `scripts/check-contrast.mjs`, `scripts/contrast-baseline.json`, `scripts/list-raw-color-backlog.mjs`, `src/app/dev/styleguide/page.tsx`, `playwright.config.ts`, `tests/visual/*`, `eslint-rules/no-raw-color.mjs`, `eslint-rules/raw-color-backlog.mjs`, `eslint.config.mjs`, `.github/workflows/ui-verify.yml`, `package.json`, `.gitignore`, `AI_TASK_QUEUE.md`, `DEVELOPER_TASK_QUEUE.md`, `BUILD_VALIDATION.md`, `AGENT_GUIDE.md` | Summary: Stood up the automated visual+a11y+contrast net BEFORE the token re-architecture so theme/style regressions are caught without manual visual QA. **Contrast:** `check-contrast.mjs` resolves every semantic fg/bg token pair (follows `var()` indirection) in both themes vs WCAG AA — all 52 pass; baseline locked at 0 (confirms dark-mode issue is cohesion, not text contrast: category/game tokens have no `.dark` override so they measure identically). **Styleguide:** auth-free `/dev/styleguide` gallery of every primitive + token swatch, theme-toggleable. **Playwright** (`@playwright/test` + `@axe-core/playwright`): 54 full-page screenshot baselines (mobile/tablet/desktop x light/dark) for deterministic routes (styleguide, marketing, legal, auth) + axe scans ratcheted via `a11y-baseline.json`; all 84 green in check mode. **ESLint `realms/no-raw-color`:** hard error banning raw palette / white-black / arbitrary hex in class strings; exempts auth + UI primitives; 64-file migration backlog ratchet (escaped `[id]` route globs). `npm run verify` + `.github/workflows/ui-verify.yml` wire it all as hard-blocking gates. Added TASK-383 (done, DEV-V-011), TASK-384 (fix a11y baseline violations: near-global `aria-prohibited-attr`, `/library` dark contrast, `/privacy` link-in-text-block), TASK-385 (authenticated-surface baselines), DEV-002/003 (CI secrets + Linux baseline seeding + branch protection + test user). Build/lint/typecheck/verify all green.

- 2026-06-26 | agent | Session: Docs hard-slim (TASK-382) | files: `AI_TASK_QUEUE.md`, `ai/archive/*`, `src/docs/human/*`, `.cursorignore`, `AGENTS.md`, `docs/README.md`, `AGENT_GUIDE.md` | Summary: Completed hard-slim pass. Active queue reduced from ~76 entries to 10 (3 partial, 1 human-only, 1 blocked, 5 not-started, TASK-382 done). Full queue backup â†’ `archive/AI_TASK_QUEUE_FULL_BACKUP_2026-06-26.md`. Moved June audits + root CODEBASE_AUDIT to archive; human onboarding/UI catalog/admin/codex/UX docs â†’ `src/docs/human/`. Added `archive/HISTORY_INDEX.md` and `.cursorignore` for archive + human paths. Updated active doc pointers; no stale FULL/SYSTEMATIC links in agent workflow.

- 2026-06-26 | agent | Session: Remediation full review + BE-04 proper close | files: `sql/supabase-be04-campaign-authz-private-schema-2026-06.sql`, `library-selectable-builders.ts` | Summary: Full front-to-back review of the audit remediation. Verified live: build/test(17)/lint(0 err) green; Wave 0 deletions real (stale editor index gave false positives â€” `Test-Path`/build are authoritative); SEC-01/04/05 in place; no code references the dropped `campaigns.memberIds`; calculator/util consolidation single-definition; AUTH-01 edge gate correct + proxy matcher covers it. **Fixed BE-04 properly**: the prior anon-only RPC revoke left `auth_is_campaign_owner`/`auth_is_campaign_participant` callable by `authenticated` (security advisor 0029). Relocated both SECURITY DEFINER helpers to a non-exposed `private` schema and repointed all 8 dependent policies (campaign_members Ã—3, campaign_rolls Ã—3, campaigns, characters); granted EXECUTE to `authenticated`; dropped the public copies. Verified via impersonated reads: participant sees campaign/members/50 rolls/characters, non-member sees 0, no recursion. Security advisor now clean except BE-09 (leaked-password = Dashboard `DEV-001`); performance advisor only INFO unused-index. Removed a dead `formatActionTypeForDisplay` import. Deliberately NOT done (documented): TOKEN-01 page-level color sweep (per-instance judgment â€” many `-600` are hover/dark-paired), HYG-01 (`photoURL` is the active avatar field; Firestore-`{seconds}` is a defensive legacy shim; retyping `fetchCodex` cascades across all `useCodex*`), DUP-05/08/11 + collapsible consolidation and BIG-01/02 decomposition (wide refactors on untested critical flows â€” need QA). Build/test/lint green.

- 2026-06-26 | agent | Session: Docs slim/current-truth pass + deferred-work planning | files: `AGENTS.md`, `docs/README.md`, `ai/AGENT_GUIDE.md`, `ai/FEATURE_INDEX.md`, `MOBILE_UX.md`, `ai/BUILD_VALIDATION.md`, `ai/AI_TASK_QUEUE.md`, `ai/archive/README.md`, `ai/FULL_AUDIT_2026-06.md`, `ai/SYSTEMATIC_AUDIT_2026-06.md`, `ai/REMEDIATION_STATUS_2026-06.md` | Summary: Reconciled active docs with post-remediation reality: removed stale route/component references (`/encounter-tracker`, `/crafting/new`, `ItemList`, `ui/collapsible.tsx`, memberIds wording in validation); added `REMEDIATION_STATUS_2026-06.md` as the active current-state doc; reclassified FULL/SYSTEMATIC audits as historical snapshots and updated source-of-truth pointers. Added concrete follow-up tasks for deferred items: `TASK-378` (HYG-01 codex typing+compat gates), `TASK-379` (DUP-05/08 pipeline+modal unification), `TASK-380` (DUP-11 CreatorPageShell + collapsible consolidation), `TASK-381` (BIG-01/02 phased decomposition with QA gates), `TASK-382` (docs compaction pass).

- 2026-06-26 | agent | Session: Remediation Wave 3 DB migrations + production deploy | files: `sql/supabase-be01-unify-user-identity-2026-06.sql`, `sql/supabase-be02-membership-single-source-2026-06.sql`, `campaigns/actions.ts`, `(auth)/actions.ts`, `lib/supabase/middleware.ts` | Summary: Backed up DB+storage, then applied live (lbqhiwudvifmkjtkccdg): **BE-01** crafting_sessions/user_enhanced_items `user_id` uuidâ†’text FK `user_profiles` (matches all user_* tables; 0 orphans; no app change). **BE-02/06** campaign membership single-sourced on `campaign_members`: backfilled non-owner members, rewrote `auth_is_campaign_participant` + campaigns/campaign_rolls RLS off `memberIds` (recursion-verified via SECURITY DEFINER helpers), updated actions to stop reading/writing the column, deployed, then dropped `campaigns.memberIds`. Advisors: only the 3 known WARNs (2 campaign authz SECDEF helpers RLS depends on, leaked-password protection = dashboard Auth setting). **AUTH-01** centralized edge auth gate. Pushed `ee82dac9` â†’ Vercel production READY (realmsrpg.com); prior deploy retained for instant rollback. Decomposition (BIG-01/02) declined: behavior-neutral churn on untested critical sheet flows. Build/test/lint green.

- 2026-06-26 | agent | Session: Remediation Wave 3 types + Wave 4 a11y/mobile/tokens | files: `proficiencies.ts`, `encounter.ts`, `encounter-combatant-types.ts`, `unified-selection-modal.tsx`, `delete-confirm-modal.tsx`, `login-prompt-modal.tsx`, `theme-toggle.tsx`, `character-sheet-settings-modal.tsx`, `CombatantCard.tsx`, `campaigns/[id]/page.tsx`, `onboarding-tour.tsx`, `tab-summary-section.tsx`, `grid-list-row.tsx` | Summary: TYPE-01 â€” `getTrainingPointLimit` now delegates to rules-aware `calculateTrainingPoints` (algebraically identical, honours core-rules overrides). ARCH-01 â€” combatant domain types live in `src/types/encounter`; component re-exports. A11Y-01/02 â€” `titleA11y` on selection/delete/login modals; aria-labels on theme buttons, settings selects, CombatantCard remove/duplicate/condition controls, campaign description. MOBILE-01 â€” onboarding tour `fullScreenOnMobile`. TOKEN-01 â€” shared status body text `-600`â†’`-700`. Build exit 0. Deferred (high-risk, need backup+regression+QA): BE-01/BE-02/06 destructive DB migrations, TYPE-02/03 wide type refactor, Wave 4 god-file decomposition + centralized auth gating, production deploy.

- 2026-06-26 | agent | Session: Feat/trait customization audit â€” workflow artifacts | files: `AI_TASK_QUEUE.md`, `BUILD_VALIDATION.md`, `DEVELOPER_TASK_QUEUE.md`, `ALL_FEEDBACK_CLEAN.md` | Summary: Audited today's feat/trait `customName`+`note` work against project rules. Code/types/enrichment/a11y/build all green; closed governance gaps â€” logged raw feedback, added **TASK-377** (done, with build_validation), new **DEV-V-010** QA suite (T001â€“T004) indexed in developer queue. No code changes; build still exit 0.

- 2026-06-26 | agent | Session: Character sheet power rolls + reaction action labels | files: `calculations.ts`, `library-entity-rows.tsx`, `library-section-props.ts`, `action-type.ts`, `power-calc.ts`, `creature-stat-block.tsx`, campaign view | Summary: Power damage rolls include Power Attack Bonus; action column shows full reaction type (Free/Basic Reaction) via `formatSavedActionTypeForDisplay`; fixed `derivePowerDisplay` collapsing reactions. Build exit 0.

- 2026-06-26 | agent | Session: Character sheet auto-proficiency on add power/technique | files: `map-selection.ts`, `add-library-item-modal.tsx`, `use-character-sheet-actions.ts`, `use-add-library-item-data.ts` | Summary: Enrich parts with codex TP on add; sync missing proficiencies when codex loads; fix add handlers to use setCharacter updater. Build exit 0.

- 2026-06-26 | agent | Session: Character creator audit verification | files: `skills-step.tsx`, `equipment-step.tsx`, `character-creator-validation.ts`, `CHARACTER_CREATOR_AUDIT_2026-06-26.md`, `SYSTEMATIC_AUDIT_2026-06.md` | Summary: Re-audit found Skills/Equipment missing Continue gating and HE validation wrongly on abilities step; fixed. Build exit 0.

- 2026-06-26 | agent | Session: Character creator audit implementation | files: `archetype-step.tsx`, `species-step.tsx`, `species-modal.tsx`, `feats-step.tsx`, `ancestry-step.tsx`, `creator-tab-bar.tsx`, `finalize-step.tsx`, `character-creator-store.ts`, `character-creator-validation.ts`, `api-client.ts`, `CHARACTER_CREATOR_AUDIT_2026-06-26.md` | Summary: Completed all CC-26 audit items â€” sticky archetype confirm, a11y/touch targets, HE overspend validation, persist schema version, apiUpload portrait, login return path with query params, cloneInitialDraft on reset. Build exit 0.

- 2026-06-26 | agent | Session: Character creator sticky footers + localized audit | files: `creator-step-footer.tsx`, all creator steps, `species-modal.tsx`, `skills-allocation-page.tsx`, `CHARACTER_CREATOR_AUDIT_2026-06-26.md` | Summary: Shared CreatorStepFooter on all steps; SpeciesModal flexLayout sticky footer with Pick Me on right; skills allocation sticky footer; localized 10-lens audit doc. Build exit 0.
