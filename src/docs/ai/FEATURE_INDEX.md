# Feature Index — "Does this already exist?"

**Purpose:** The anti-re-implementation guardrail. Before building a new component, hook, service, route, or util, scan this index first. Most accidental duplication in this repo came from agents not knowing a feature already existed.

**Also check:** generated barrel inventory [`FEATURE_INDEX_BARRELS.generated.md`](FEATURE_INDEX_BARRELS.generated.md) (`npm run tasks:generate-index` after changing barrel exports). CI fails if that file is stale.

**How to use:** Find your topic below → open the listed file/barrel → extend it (add a prop/variant) rather than forking a parallel copy. If you add a new top-level feature/hook/shared component, add a line here **and** regenerate the barrel file if you changed an index.ts export.

> This is a map, not the source of truth for behavior. For schema use `SUPABASE_SCHEMA.md`, for formulas `GAME_RULES.md`, for data flow `ARCHITECTURE.md`.

---

## Pages / features (`src/app/(main)/*`)

| Feature | Route / page |
|---------|--------------|
| Home / marketing landing | `(main)/page.tsx` (RSC; `HeroSection` is the client island) + `components/landing/` — copy SoT `lib/constants/copy/landing-copy.ts`. Guest primary CTA **Create Character** → `/characters/new` (repeated after how-it-works). Steps: Create a character → Find a table (Discord invite) → Start playing (TASK-763). Closing tertiary **Join the Discord**. Do not reintroduce a root App Router page that wraps `(main)/layout` (chrome remount). |
| Character list / dashboard | `characters/page.tsx` — square portrait cards (1:1 crop); no search/ListHeader |
| Character sheet (view + edit) | `characters/[id]/page.tsx` facade (TASK-666d) + `use-character-sheet-page.ts` (~288) composing `use-character-sheet-page-data` / `use-character-sheet-page-ui`; document SoT `useCharacter` + `setQueryData` on `characterKeys.detail` (TASK-750); other-user GET uses referenced `enrichment` and skips catalog hooks (TASK-773); owner sheet keeps catalog hooks for add-X; layout `CharacterSheetBody` (single library mount; mobile side-scroll: `basis-full` + `gap-4` + PageContainer-matched `scroll-px-*` gutters — TASK-538); derived `useCharacterSheetDerived`; handlers `useCharacterSheetActions`; Library card title peer of Skills/Archetype (`text-lg`); library lists via `entity-library-sections` + `library-entity-rows` (powers/techniques: Energy header + far-right spend `rightSlot` via `CHARACTER_SHEET_ENERGY_SPEND_ROW_CHROME`; no static mid-row Energy value column); Library card facade `library-section.tsx` (~406 LOC) + `library-powers-panel` / `library-inventory-panel` / `use-library-section-rows` / `use-library-tab-navigation` / `library-tab-config` (`LIBRARY_TAB_DEFS` SoT — TASK-598); LibrarySection chrome props + `SheetLibraryModel` via context (TASK-667; resolved via `buildLibrarySectionData`); `CharacterSheetModals` context-only; thin stats shim `characters/[id]/character-sheet-utils.ts` → `lib/game/calculations`; feats tab via `FeatsTraitsListSection` + `library-feat-rows` (edit feat rank via expanded Feat Levels chips, not a Lvl stepper — TASK-780; player feat/trait `customName` italic title + play `note` in GLR `descriptionAfter` — TASK-783; trait map `traitCustomizations`); library tab clamp `resolveLibraryActiveTab`; create-time tab hide via `defaultLibraryTabVisibilityForArchetype` (`libraryTabVisibility`); edit-archetype remount via `editArchetypeSessionKey`; post-save sheet tour + level-up guides via `components/onboarding/` + `lib/onboarding-preferences.ts` (TASK-388) |
| Character sheet Library → Powers | `character-sheet/library-powers-panel.tsx` — Innate Energy summary and Innate Powers heading reuse global `innateEnergyHelp` / `innatePowersHelp`; `PowersListSection.titleAddon` passes help through shared `SectionHeader` (TASK-733) |
| Character creator | `character-creator/` (wizard steps under `components/character-creator/steps/`) |
| Character creator entry (Guided / Custom / Legacy) | `characters/new/page.tsx` — copy in `GUIDED_CREATOR_COPY.chooser`. Custom card is player-facing (no Layer 3 / cohesive-creator jargon) — TASK-784. Legacy card → `/characters/new/advanced` (user-facing **Legacy**, not Advanced) |
| Guided character preview strip/panel (TASK-686, TASK-694) | `character-preview-panel.tsx` + `lib/guided-creator/preview-ability-summary.ts` — path/type once in subtitle; ability chips gated until Abilities selection/completion; all six signed on strip (horizontal scroll on mobile); only `pow_abil`/`mart_abil` use power/martial chip highlight; feat labels via `indexByNormalizedIds` (no raw id fallback — TASK-732) |
| Cohesive character creator (guided shell) | `characters/new/guided/page.tsx`, `components/guided-creator/` (incl. `GuidedPathCustomArchetype`, Path L1↔L3), `stores/guided-creator-store.ts` (`pathLayer` only in draft — **no persisted `creationMode`**; rail/Reveal completion derived from `lib/guided-creator/substep-satisfaction.ts`, schema v15 adds `defenseVals` + `skillAbilities` (TASK-790) after v14 `powersPhase` (TASK-756) and v13 `clientRequestId`; save uses `archetypePathId` when a path was picked), `lib/guided-creator/build-character.ts`, chooser **Guided** → Path L1 / **Custom** → `?entry=custom` Path L3 (TASK-638). **Your Hero** chips: Loadout via `resolveDraftArmaments` / `buildEquipmentLookup`; powers/techniques via `mergeLibraryBySource` + `indexByNormalizedIds` (official + user; not official-id Map) — TASK-730. Path **More details** equipment via `mergeLibraryBySource` + `buildEquipmentLookup` (official + user + `docId`) — TASK-732 |
| Selection grammar (cards ↔ GridListRow; entity depth vs catalog breadth) | `REALMS_PRODUCT_OVERVIEW.md` §3.1; `guide/04-floating-ui-tooltips.md` § Related patterns; components: `GuidedChoiceCard`, `GuidedEntityDetailModal`, `GridListRow`, `GuidedLayerNav`, `DetailOptionList` |
| Guided choice-card deep-dive (TASK-432+) | **See more…** = in-card deepen (truncated copy, expandedExtra / equipment mechanic chips); **More details** = entity modal (`onDetails` → `GuidedEntityDetailModal`, path/species Close \| Select via TASK-448); **See more options** = catalog Layer 2 (`GuidedLayerNav` / browse). Do not invent specialist verbs. Shared rows: `DetailOptionList` + `@/lib/detail-option`. Wrappers: `GuidedTraitOptionList` / `GuidedDetailOptionList`. Granted overview traits: `GuidedChoiceCard` `readOnly` (no select affordance; TASK-711). **See more / See less** stop card-select keys (target must be the card root); selection is announced in `aria-label` (“…, selected”) — no `aria-selected` on a generic div (TASK-734 / audit 03 P1-5). |
| Guided skills suggestions (ability-tier curation) | `lib/guided-creator/curated-skills.ts` (`curateGuidedSkillIds`, `getGuidedAbilityRecommendationTiers`, `formatGuidedSkillAbilityTag`), `guided-skill-recommendations.ts` (`buildGuidedSkillSuggestions`); consumed by `steps/skills-step.tsx` + `AddSkillModal` badges via `guided-skills-panel.tsx`. L1 rows: name+chevron+chips inline (TASK-704); contributing Ability chip or governing-Ability picker (multi-ability) + Skill Bonus `InfoTippy` (`getGuidedSkillBonusHelp` in `tooltip-text.tsx`) — TASK-548 / TASK-790. Defense Bonuses via shared `DefenseBonusesCard`; persist `defenseVals` + `skillAbilities` on save. |
| Guided feat Layer 2 add modal | `guided-feats-l2-modal.tsx`, `lib/guided-creator/feats-l2.ts`, `feat-selection.ts`; wired in `archetype-feats-step.tsx` + `character-feat-step.tsx` via `GuidedLayerNav` → `UnifiedSelectionModal` (TASK-565; same grammar as skills/loadout/powers L2). Creator headers/cells compose Codex `featSelectable*` helpers but omit Req. Level because unmet `lvl_req` is hidden; Codex/Admin keep it (TASK-758). `GuidedFeatsFilterFields` composes Category, State Feats (shared restriction InfoTippy, TASK-759), and **Archetype Path** last (`usePathListFilter` / ADR-0014). Path flow See more auto-selects every player-visible path of `draft.archetypeType` with Filters expanded; custom/no-path inline catalogs get the control with no auto-select. Path-name chips replace Recommended while filtering; family ranks of a matching feat stay (TASK-753). |
| Guided L1 curated feat eligibility | `selectableCuratedFeatIds` (`lib/guided-creator/feat-selection.ts`) — path-curated cards honour `checkFeatRequirements` like the L2/L3 catalog, keeping an already-selected unmet pick visible so it stays deselectable. Used by both feat steps; the character-feat step no longer auto-picks the first curated feat (TASK-738 / report 03 P1-10; vitest) |
| Guided L3 inline catalog (no path — full Customize) | `shared/guided-choice/guided-inline-catalog-list.tsx` → **`GuidedInlineCatalogList`** (shared). Gate: `prefersDeepCatalogEntry(draft)`. Wired on archetype-feats, character-feat, loadout, powers/techniques (TASK-684). TASK-685: unmet feats hidden; custom loadout `fullCatalog` phases (weapons always; armor skipped for Power only); gear quantity-first (far-right stepper replaces +); sequential innate → powers → techniques screens (TASK-756; no innate-scope filter) + theoretical max EN. **TASK-686–690:** preview all six abilities + custom type chip; weapon/armor columns from `ARMAMENT_LIBRARY_CONFIG`; gear Category/Rarity/Currency (TASK-724); Power armor skip ignores path `armorStep`; powers Energy kind-correct. **TASK-709 / TASK-758:** L3/L2 GLR columns/expand match Library/Codex builders (`featSelectableColumns`, `officialPowerRowColumns` / `officialTechniqueRowColumns`, `armamentRowColumns` + Official property sections), with the creator-only Req. Level omission registered in ADR-0009; SourceFilter + Create Armament on loadout; allowlist in ADR-0012. **TASK-727:** L3 innate catalog stays populated at Innate Energy cap; select swaps last-in until the pick fits (`applyInnateSelection`). Path L2 innate USM add-path uses the same helper (`nextSelectedIds`). **TASK-728:** Selected panel still mounts only when N>0; insert height is `scrollBy`-compensated (`lib/utils/stabilize-vertical-scroll.ts`) so the clicked row does not jump. GLR surfaces `guided-*-l3`. |
| Guided equipment phased sub-flow (TASK-424 / 442 / 443 / 446 / 447 / 456 / 457 / 458 / 460 / 461 / 464–468 / 527 / **689** / **688** / **705** / **709** / **TASK-724** / **TASK-757**) | `GUIDED_EQUIPMENT_PHASED_SPEC.md`, `lib/guided-creator/equipment-eligibility.ts` (`resolveArmorStepMode`: Power always `none`), `equipment-phase-stats.ts`, `equipment-currency.ts`, `equipment-phase-nav.ts`, `loadout-tp.ts`, `equipment-catalog-rows.ts`, `equipment-phase-candidates.ts`, `guided-equipment-l2.ts` (cells via `armamentRowColumns`; expand via Official `buildOfficialItemRows` parts + `propertiesProficienciesSection`; **gear Category** taxonomy column — TASK-724), `guided-creator/guided-equipment-l1-phase.tsx`, `guided-equipment-phase-layout.tsx`, `guided-equipment-fact-chips.tsx`, `shared/loadout-budget-bar.tsx`, `guided-equipment-l2-modal.tsx`, `guided-equipment-l2-grid.ts` (reuses `ARMAMENT_LIBRARY_CONFIG`; gear headers Name/Category/Rarity/Currency), `lib/game/archetype-path.ts` (`parseLevel1LoadoutsField` metadata), `hooks/use-guided-equipment-catalog.ts` (`mergeLibraryBySource`), `lib/library/source-scope.ts`, `lib/game/weapon-attack-ability.ts`, `steps/loadout-step.tsx`. Card-first L1; optional picks; shared **`LoadoutBudgetBar`**; L2/L3 Codex-parity columns; USM qty slot `7.5rem`; Power never shows armor. **TASK-705:** L3 SourceFilter (default All) + Create Armament hatch → `/item-creator`; path L2 SourceFilter default Realms. **TASK-757:** a path-based Power gear screen that skipped the weapon phase has a bottom-right **See weapons** hatch on the same `GuidedLayerNav` row as See more (trailing expand) into the existing capped weapon L2 modal; Custom/fullCatalog and flows that already showed weapons do not duplicate it. |
| Guided powers/techniques L1 + L2 (TASK-444 / TASK-456 / TASK-458 / TASK-461 / TASK-463 / TASK-470–472 / TASK-573 / **TASK-610** / **TASK-615** / **TASK-687** / **TASK-691** / **TASK-709** / **TASK-727** / **TASK-726** / **TASK-756**) | `steps/powers-techniques-step.tsx` (facade; inner `powersPhase` screens), `lib/guided-creator/powers-phase-nav.ts`, `steps/use-powers-techniques-selection.ts`, `guided-powers-techniques-l1-content.tsx`, `lib/guided-creator/powers-techniques-step-helpers.ts` (`applyInnateSelection` swap-at-energy-cap +test; `buildLookup` via `indexByNormalizedIds` + name), `guided-powers-techniques-l2-modal.tsx` (`InnateEnergyPointStatus` + `InnatePowersHelpTip`), `powers-techniques-l1-candidates.ts`, `powers-techniques-l2.ts` (`buildOfficialPowerRows` / `buildOfficialTechniqueRows` for L2/L3 columns; L1 cards still `buildPowerTechniqueBudgetDisplay`), `powers-techniques-energy-filter.ts`, `power-technique-display.ts` → thin wrappers over `library-selectable-builders`; Energy kind-aware (`techniques` vs `powers`); theoretical max EN uses `pow_abil` / `mart_abil` by track; L3 innate list stays at energy cap (threshold/TP still hide); path L2 innate USM swaps last-in via `nextSelectedIds` → `applyInnateSelection`; Innate Powers heading + Innate Energy tracker InfoTippys (`innatePowersHelp` / `innateEnergyHelp`); GLR `guided-powers-l3` / `guided-techniques-l3`. |
| Legacy character creator (classic 9-step; route `/advanced`) | `characters/new/advanced/page.tsx` (header **Legacy** chip + `GUIDED_CREATOR_COPY.legacyWizard`; TASK-748), `stores/character-creator-store.ts` (`getCharacter` sets `libraryTabVisibility` via `defaultLibraryTabVisibilityForArchetype`; persist `migrateCharacterCreatorPersistedState` is field-by-field — schema v4 does not wipe in-progress drafts, TASK-740; `clientRequestId` on the draft for create idempotency, TASK-738; persist currency via `clampSavedCurrency` so a character cannot start in debt, TASK-739); step facades in `character-creator/steps/*-step.tsx` (barrel `steps/index.ts`). TASK-598: `finalize-step` → `steps/finalize/*` (portrait → shared `CreatorPortraitUpload`); `feats-step` → `steps/feats/*`; `equipment-step` → `steps/equipment/*`; `powers-step` → `steps/powers/*` + `lib/creator/advanced-powers-selectable.ts` (named exports unchanged). |
| Advanced equipment catalog / budget (TASK-596 / TASK-606) | `lib/creator/advanced-equipment-catalog.ts` (+test) — merge user/codex/public rows, path recommend resolve, TP/unarmed helpers, inventory add/remove/replace; wired by `character-creator/steps/equipment-step.tsx` facade + `steps/equipment/` UI panels (list-columns, unarmed-prowess-panel, path-loadout-section, selected-equipment-list, equipment-catalog-panel, step-header). Starting/spend currency from guided `equipment-currency` (imported by step). Budget chrome: shared **`LoadoutBudgetBar`** → PointStatus (same as Guided; not USM). |
| Advanced powers selectable builders (TASK-598) | `lib/creator/advanced-powers-selectable.ts` (+test) — power/technique/empowered SelectableItem builders + library merge/dedupe; uses sheet `buildEmpoweredPowerSelectableItem` (no fork); wired by `powers-step` facade + `steps/powers/` panels/modals. |
| Library (user + official content browse) | `library/page.tsx` — tab badges from `GET /api/user/library/counts` / `GET /api/official/counts` (`useUserLibraryCounts` / `useOfficialLibraryCounts`); row lists fetch the active tab only (TASK-774 / ADR-0015). Armaments split into **Weapons / Armor / Shields** tabs (type-specific columns in `lib/library/official-item-list.ts`; armor includes Abl. Req. + Crit +); labels in `lib/library/armament-library-labels.ts` |
| My Library entity tabs (sync/duplicate shell) | `library/components/UserLibraryEntityTabShell.tsx` (`enableSync` default true; `enableSync={false}` = list chrome only — no sync-all **or** duplicate modals), `library/hooks/use-library-entity-sync.ts`, `library/hooks/use-library-duplicate-confirm.ts` — sync tabs: `LibraryItemsTab` / `LibraryPowersTab` / `LibraryTechniquesTab` / `LibraryCreaturesTab`; basic: `LibraryEnhancedTab` (ADR-0001 / TASK-475) |
| Codex (rules data browser) | `codex/page.tsx` |
| Realms Library, guest read-only | `library/page.tsx` + `library/LibraryPublicContent.tsx` — guests see official "Realms" content with the My-Library toggle + "Add to library" hidden. (Former `/browse` was a redundant duplicate; removed and redirected to `/library` — TASK-336.) |
| Power creator | `power-creator/page.tsx` (shell + bootstrap) + `use-power-creator-workspace.ts` + `power-creator-editor.tsx` + co-located `power-creator-cost-derivation.ts` / `power-creator-part-actions.ts` / editor section modules + `power-creator-help.tsx` (TASK-381, **TASK-616**, **TASK-408**) — chrome: `CreatorPageShell` + `CollapsibleSection` (no empty expanded summary line — TASK-764). Guided power creator **not shipped** — deferred WAITING TASK-410–414 (owner 2026-08-14). |
| Technique creator | `technique-creator/page.tsx` (shell + bootstrap) + `use-technique-creator-workspace.ts` + `technique-creator-editor.tsx` (TASK-601) — chrome: `CreatorPageShell` |
| Empowered technique creator | `empowered-technique-creator/page.tsx` (shell) + `use-empowered-technique-creator-workspace.ts` + `empowered-technique-creator-editor.tsx` + co-located `empowered-technique-cost-derivation.ts` / `empowered-technique-part-actions.ts` / editor section modules (TASK-601, **TASK-610**, **TASK-683**) — Attack mode uses cheaper-EN overlap (`pickCheaperEnPart`) + No Attack; chrome: `CreatorPageShell` |
| Item (armament) creator | `item-creator/page.tsx` (shell + bootstrap) + `use-item-creator-workspace.ts` + `item-creator-editor.tsx` + co-located `item-creator-cost-derivation.ts` / `item-creator-property-actions.ts` / editor section modules (TASK-381, **TASK-616**) — chrome: `CreatorPageShell` |
| Species creator | `species-creator/page.tsx` (shell) + `use-species-creator-workspace.ts` + `species-creator-editor.tsx` / bootstrap (TASK-601); Load ungated — chrome: `CreatorPageShell`; skill labels via `findByNormalizedId` (unmatched ids omitted, not raw UUID — TASK-732) |
| Creature creator | `creature-creator/page.tsx` (shell) + `use-creature-creator-workspace.ts` + `creature-creator-workspace-persistence.ts` + `creature-creator-editor.tsx` + co-located `creature-creator-{library-selectables,derived-stats,feat-armament-display,skill-bonus,summaries}.ts` + editor section modules (TASK-381, **TASK-610**, **TASK-615**, **TASK-712**) + `creature-skill-utils.ts`; Load/library pickers use `mergeLibraryBySource` (`lib/library/source-scope.ts`; public wins on id). Armament still hides selected `docId`s; empowered still dedupes on `docId`. `CreatorPageShell` + reset confirm |
| Crafting (sessions + enhanced items) | `crafting/layout.tsx` title/description (TASK-793); `crafting/page.tsx`, `crafting/[id]/page.tsx` facade (TASK-607 / **TASK-666e**) + `_components/` (`use-crafting-tool-page`, `crafting-tool-helpers`, `crafting-tool-derived`, summary sidebar, item-options / adjustments / rolls / optional-rules / outcome panels); item picker `components/crafting/CraftingItemSelectModal` |
| Encounters | `encounters/page.tsx`, `encounters/[id]/page.tsx`, `encounters/[id]/combat/page.tsx`, `encounters/[id]/mixed/page.tsx`, `encounters/[id]/skill/page.tsx`; play views `CombatEncounterView` / `SkillEncounterView` facades (TASK-608) + `_components/combat/` (`use-combat-encounter-view` facade ~78 LOC — **TASK-666a** + `use-combat-{linked-character-sync,roster-actions,round-actions}`, helpers, round controls, combatant list, add sidebar) + `_components/skill/` (`use-skill-encounter-view`, trackers, participant list/card, sidebar) + shared `encounter-view-helpers`; `CombatantCard` facade ~149 LOC (**TASK-618**) + `combatant-card-{helpers,initiative,header,resources,conditions,quick-actions}`; participant picker remains **`AddCombatantModal`**. Linked-character HP/EN/AP sync is `useCampaignCharacterEncounters` / `campaignKeys.characterEncounter` → `getCampaignCharacterForEncounter` (`?scope=encounter`; **TASK-762**) — not `useCampaignCharacterView` / the RM-view GET. Add Combatant and add-all use `fetchCampaignCharacterForEncounter` (same fetcher).
| Campaigns | `campaigns/page.tsx`; detail `campaigns/[id]/page.tsx` facade (**TASK-666c**) + `_components/` (`use-campaign-detail-page`, header / invite / roster / roll-log sections, `character-chip`, `add-character-modal`); RM character view `campaigns/[id]/view/[userId]/[characterId]/page.tsx` — read-only `useCharacterSheetDerived` + `CharacterSheetBody` (TASK-597); load is `useCampaignCharacterView` / `campaignKeys.characterView` → `getCampaignCharacterForView` (**TASK-761**; campaign-scoped + viewer-segmented key — not `characterKeys.detail` / `/api/characters/[id]`, which does not enforce roster + RM auth). Full RM GET returns referenced `libraryForView` + `enrichment` (**TASK-773**); the page does not fetch the viewer's library or official catalogs. `?scope=encounter` is still the minimal HP/EN/AP GET (TASK-762). |
| Admin hub | `admin/page.tsx` — links Codex editor, public-library, images, users, roles, core-rules, changelogs (no tooltips admin) |
| Account / profile | `my-account/layout.tsx` title + noindex (TASK-793); `my-account/page.tsx` facade (TASK-666) + `_components/` (`use-my-account-page`, helpers, role/profile/preferences/security/danger cards); tutorials on/off (TASK-388 localStorage) |
| Post-activation onboarding (play together, sheet tour, level-up guides) | `components/onboarding/` (`PlayTogetherModal`, `SheetTourOfferModal`, `SheetTour`, `LevelUpGuideCard`); prefs `lib/onboarding-preferences.ts`; guide builder `lib/level-up-guide.ts`; highlight helper `lib/sheet-tour-highlight.ts` (`ONBOARDING_FLOATING_CARD_CLASS`); retake tour via `CharacterSheetSettingsModal`; wired from guided `reveal-step` + advanced `finalize-step` + sheet page |
| Rules / resources / about / privacy / terms | `rules` (crawlable `RULES_COPY.seoDescription` above the Google Doc iframe — TASK-793; full MDX rulebook is WAITING TASK-796), `resources`, `about` (RSC + `AboutCarouselSection` island — TASK-789), `privacy`, `terms` |
| Site / marketing copy (edit prose here) | `lib/constants/copy/*-copy.ts` via `lib/constants/site-copy.ts` (landing, auth, about + carousel, nav, rules, resources, privacy, terms, footer, guided creator, onboarding, archetype category Power/Martial/P-M; Discord URL + motto + contact email in `shared-copy.ts`). Tooltips: `public/tooltip-text.tsx`. Screenshot audit: `playwright.site-copy-audit.config.ts`. |
| Root layout + Vercel Web Analytics | `src/app/layout.tsx` — fonts/metadata/providers; `<Analytics />` from `@vercel/analytics/next` (TASK-765). Enable in Vercel Dashboard (DEV-006); CSP hosts in `next.config.ts`; ops notes in `DEPLOYMENT_AND_SECRETS_SUPABASE.md` Step 3b |
| Canonical site URL + SEO | `lib/site-url.ts` (`getCanonicalSiteUrl` / `NEXT_PUBLIC_SITE_URL` fallback `SITE_URL` = `https://realmsrpg.com`; `isProductionIndexingEnabled` noindexes preview/local); root `metadataBase` + Open Graph + Twitter + `alternates.canonical`; `opengraph-image.tsx`; `robots.ts` (`ROBOTS_DISALLOW`) + `sitemap.ts` (`SITEMAP_PATHS`); auth + admin + `/dev/styleguide` `robots: noindex` (TASK-771) |
| Archetype category titles + descriptions (Power / Powered-Martial / Martial) | `lib/constants/copy/archetype-category-copy.ts` (`ARCHETYPE_CATEGORY_INFO`) — consumed by sheet Edit Archetype, Advanced forge step, creature `ArchetypeSelector` (icons stay local) |

## Data hooks (`src/hooks/`, import from `@/hooks`)

| Need | Hook(s) |
|------|---------|
| Auth / current user | `useAuth`, `useAuthStore`, `useProfile`, `useAccountProfile`, `useAdmin` — `useAuth` drops stale `getUser` after `onAuthStateChange` and `queryClient.clear()` on `SIGNED_IN` / `SIGNED_OUT` / `USER_UPDATED` (not `INITIAL_SESSION`) (TASK-737) |
| Codex rules data (feats, skills, species, traits, parts, equipment, properties, archetypes) | The `useCodex*` family (aliased `useSkills`/`useSpecies`/`usePowerParts`/etc.) — one collection each via `codexKeys.collection(key)` / `fetchCodexCollection` → `GET /api/codex?collection=` (TASK-775 / ADR-0015). Rows mapped by `lib/codex/row-map.ts` (same module as view enrichment; TASK-777; archetype path join stays in the route). `useCodexParts` / `usePowerParts` / `useTechniqueParts` share `['codex', 'parts']` and split with `lib/codex/part-type.ts`. `useCodexFull` (`['codex']`, whole payload) is the admin spreadsheet only. Creators use the collection hooks. `['codex']` invalidation still reaches every slice by prefix |
| Game rules constants | `useGameRules` (`['codex', 'coreRules']` — not the full payload, TASK-775), `getGameRulesFallback` |
| Trait / skill id↔name resolution | `resolveTraitIds`, `resolveSkillIdsToNames`; skill maps via `buildSkillIdToName` (`lib/codex/skill-list`) |
| Official library (browse + add to my library) | `useOfficialLibrary`, `useOfficialLibraryCounts`, `useAddOfficialToLibrary` (`hooks/use-official-library.ts`) |
| User-created content (CRUD: powers/techniques/items/creatures/species) | `useUser*`, `useUserLibraryCounts`, `useDelete*`, `useDuplicate*` from `use-user-library` |
| Characters CRUD | `useCharacters`, `useCharacter`, `useSaveCharacter`, `useDeleteCharacter`, `useDuplicateCharacter`; query keys scoped by signed-in user id (`characterViewerId` / TASK-741). Sheet document SoT is `useCharacter` / `characterKeys.detail` (TASK-750): `setCharacter` is `patchCharacterDetailQuery`; `useSaveCharacter` merges the **applied** PATCH body (original dirty or 409 retry) + `updatedAt` into that cache and invalidates the list only (not detail). `useSaveCharacter` PATCHes via `saveCharacterWithConflictRetry` (optional `mergeOnConflict` re-applies onto the remote document — library add, TASK-746). Create via `createCharacter` (`character-service`) — pass `{ clientRequestId }` (uuid persisted on the creator draft, reused by retries, cleared by `resetCreator`) so a lost response or reload-then-retry cannot insert twice (TASK-738). **PATCH** is dirty-key + `updatedAt` lock (ADR-0013 / TASK-741): omitted keys stay stored; stale token → 409, client refetches and retries. Same-tab queue: `lib/character/save-lock.ts` (TASK-786). Cross-user GET uses `resolveCharacterVisibility` (column first, blob fallback) in `lib/character-list-columns.ts` (TASK-735). Other-user GET returns referenced `libraryForView` + `enrichment` (TASK-773); owner GET omits both so the sheet keeps catalog hooks for add-X. |
| Campaigns | `useCampaigns*` (`useCampaignsFull` gated on signed-in user — TASK-737), `useCampaign`, `useCampaignRolls`. RM character view: `useCampaignCharacterView` / `campaignKeys.characterView` → `getCampaignCharacterForView` (campaign route, not `characterKeys.detail` — TASK-761). Payload includes referenced `libraryForView` + `enrichment` (TASK-773). Combat/add `?scope=encounter`: `useCampaignCharacterEncounters` / `fetchCampaignCharacterForEncounter` / `campaignKeys.characterEncounter` → `getCampaignCharacterForEncounter` (minimal HP/EN/AP; not the RM-view GET — TASK-762) |
| Encounters | `useEncounters`, `useEncounter`, `useCreate/Save/DeleteEncounter` |
| Crafting + enhanced items | `useCraftingSession(s)`, `useEnhancedItems` + CRUD |
| Save a creator's output | `useCreatorSave`; load into a modal: `useLoadModalLibrary` (incl. species/creature + `prefetch`) |
| Character creator path recommendations | `useCreatorPathData` — resolves `path_data` from draft or codex by `archetypePathId` |
| Archetype Path list filter | `usePathRecommendationIndex` / `usePathListFilter` (`hooks/use-path-recommendation-index.ts`) — memoized off `['codex', 'archetypes']`; browse lists pass `kind` (or `POWER_LIST_PATH_KINDS` / `EQUIPMENT_LIST_PATH_KINDS`); creator See more passes `autoSelectType` |
| Guided creator path data | `useGuidedPathData` in `components/guided-creator/use-guided-path-data.ts` (returns `isLoading` while codex archetypes load for selected path — Loadout phase entry waits on it, TASK-527) |
| Autosave (debounced) | `useAutoSave` — callbacks in refs so re-renders do not reset the debounce; failed saves retry 2s/5s/15s/60s; `pagehide` / hidden tab flush dirty data (TASK-736) |
| Sheet ↔ encounter HP/EN/AP | `useCharacterResourceSync` + `lib/encounter/character-resource-sync.ts` — debounce 400ms; `skipLock` LWW; schedule only when HP/EN/AP change (`nextResourceSyncCursor`, TASK-786) |
| Client-only mount (portals / theme) | `useIsClient` — prefer over `useEffect(() => setMounted(true))` (TASK-430) |
| List sorting / modal list state | `useSort`, `sortByColumn`, `useModalListState` (rare non-USM only — AdminTraits choice-option editor chrome; TASK-572) |
| Tooltips (canonical) | `InfoTippy` (`tone`: default `info` link-blue; `tp` on Training Points PointStatus; `current` on-chip — TASK-707). Trigger layout box is the 16px icon; 44px touch is `.hit-area-layout-neutral` (does not stretch filter labels / `GuidedSectionTitle` — TASK-725) + `WordHelpTip` (word-tied, no icon) + `public/tooltip-text.tsx` (`getAbilityHelp` / `getDefenseHelp`, `defenseScoreHelp`, `armamentProficiencyHelp`, `innateEnergyHelp` / `innatePowersHelp` — TASK-726; **power creator** `powerCreator*Help` + `guidedPowerCreator*` L1 placeholders — TASK-408). Intentional domain-copy exception: `STATE_FEAT_RESTRICTION_NOTICE` in `lib/codex/feat-restriction-notice.ts` is the one sentence for L1/L2 notices and creator/Codex/Admin filter tips (TASK-759; no duplicate in tooltip-text). See **`guide/04-floating-ui-tooltips.md`**. |
| Floating UI (`@floating-ui/react`) | Engine inside `InfoTippy` only today; new anchored UI → shared primitive first — see same guide appendix |

> Codex reads live under the **`['codex']` key prefix** (see `use-codex.ts` / `DATA_HANDLING.md`): `codexKeys.all` for the full payload, `codexKeys.collection(key)` for one collection. Do not add a codex fetch outside that prefix, and do not reach for `useCodexFull` to read a single collection.

## Shared UI components (`src/components/shared/`, import from `@/components/shared`)

| Need | Component |
|------|-----------|
| Expandable list row (Library/Codex/sheet/creator) | `GridListRow` facade (TASK-611: types/columns/detail/expanded/collapsed + chrome) — **fact policy (TASK-437/454/461):** dense browse keeps `ListHeader` columns when space allows (short **TP** headers OK on L3 tables); deep-dive/`DetailOptionList` omit columns → self-describing chips. **Required-facts registry (TASK-629 / ADR-0009):** `lib/glr/required-facts-registry.ts` + `validate-glr-facts.ts`; CI in `required-facts-registry.test.ts` — add a surface row + test binding when shipping a new GLR list. **Chrome + spacing CI (TASK-631 / TASK-637 / TASK-674 / TASK-702 / TASK-710):** `lib/glr/glr-chrome-spacing-norms.ts` + `validate-glr-chrome-spacing.ts`; CI in `validate-glr-chrome-spacing.test.ts` (`npm test` + `ai-task-verifier.yml`) — register My Library tabs in `MY_LIBRARY_ENTITY_TAB_SOURCES`, Codex/Admin in `CODEX_BROWSE_SHELL_SOURCES`, USM in `USM_LIST_SHELL_SOURCES`, creator-embedded lists in `CREATOR_EMBEDDED_GLR_SOURCES`; keep shell `gap-1`, pair `rowChrome` edit/delete/leftSlot/rightSlot on **ListHeader and GridListRow** when slot content is conditional (e.g. patch sync), no `40px` grid action tracks in `GLR_GRID_COLUMN_SOURCES`. Guide: `guide/02-components-and-lists.md`. Helpers: `lib/chip/list-row-metadata.ts`, `lib/detail-option` `compact-facts` (`TP_COST_LABEL` / `TRAINING_POINTS_COST_LABEL`, `namedPropertyDescriptorChips`, combat+equipment builders). Dense GLR / ExpandableChip `costLabel` defaults to **TP** (`TP: N`); guided L1/L2 pass spelled-out Training Points. **Action chrome (TASK-622/624/630):** edit/delete all-or-nothing (never split hover); Library + OfficialEntityList + CodexBrowseListShell consumers omit leftover `40px` and use `ListHeader` `rowChrome` for edit/delete/add/rightSlot (incl. Creatures). Default list gap is shell `gap-1` — do not override with `space-y-3` on GLR lists. Suppress expanded Total TP when a TP column already exists (`columnsAlreadyShowTrainingPoints`). Parts chips: no `(0)` cost/level; `(Lv.N)` only when level is greater than 0. **Parts/Properties (TASK-583):** `MetadataDetailSection.defaultCollapsed` + `labelHelpKey` — Parts/Properties & Proficiencies start collapsed with chevron + family InfoTippy; descriptor sections stay open. **Mobile (TASK-536):** `buildMobileCollapsedGridColumns` collapses vacated `hideOnMobile` tracks below `lg` so names keep width beside X/+. Data-column templates use `--glr-desktop-grid` / `--glr-mobile-grid` — never inline `gridTemplateColumns` on that grid (inline styles override the mobile media query). Chrome stretch-grid may set its own template inline (TASK-710). **Body toggle (TASK-539):** header, mobile summary, and non-interactive expanded panel all expand/collapse (chip groups / buttons excluded). **descriptionAfter (TASK-783):** optional node inside the expanded description surface after a divider when both exist (sheet feat/trait play notes — not a second card). |
| Descriptor chip + property tip | `DescriptorChipWithTip` — non-expanding chip with **InfoTippy inside** the chip when description exists (TASK-454/465); on-chip trigger uses `tone="current"` (TASK-707); guided cards use `GuidedFactChipRow`; GridListRow descriptors use the same tip path via `GridListChip` (TASK-461) |
| Guided / Advanced Loadout budget chrome | `shared/loadout-budget-bar.tsx` → **`LoadoutBudgetBar`** — Currency (optional) + Training Points PointStatus with tip via `labelAccessory` (`tone="tp"` — TASK-707) (Guided phase layout / L2 footer / powers; Advanced equipment / powers / finalize — TASK-606 / TASK-614 / **TASK-706**). Optional `align` + `leading` (Guided Powers Innate Energy + `innateEnergyHelp` — TASK-726) + `trailing` (finalize Energy). Inline PointStatus is `text-base` (same as Skills / Ability Points). Import `@/components/shared`. |
| **Entity card art — click to enlarge (site-wide default)** | **`ExpandableImage`** (+ `ExpandableImageModal`); list thumbs: `ListRowThumbnail`. Also: sheet portrait (play), creature stat-block, campaign chips, account profile. Exceptions: `guide/03-entity-card-art.md` § Adoption inventory (TASK-478) |
| **Entity card art — list thumb** (44px, D&D Beyond style) | `GridListRow.thumbnail` + `ListRowThumbnail`; `ListHeader.hasThumbnailColumn`; resolve via `resolveListRowThumbnail` (`list-row-image.ts`). Dark placeholders: `lib/placeholder-art.ts` + `usePlaceholderTheme` in `ListRowThumbnail`. Wired for all art-capable kinds: species, creature, equipment, power, technique — Library/Official/Codex/Admin lists, selection modals, character sheet library sections, advanced + creature creator selected lists, CreatureStatBlock nested lists. Enhanced items deferred (TASK-500). No thumbs on feats/skills/archetypes/parts/etc. |
| **Portrait thumb** (campaign pickers, decorative identity) | `PortraitThumb` (`character/portrait-thumb.tsx`) — `useEffectivePortrait` + `bg-image-matte`; not expandable (nested in links/buttons). Sheet/campaign chips use `ExpandableImage` when browse-to-enlarge applies. |
| **Entity card art — choice card hero** (guided creator) | `GuidedChoiceCard` (wraps `ExpandableImage`) |
| **Entity card art — admin / creator** | **`RealmsImageField`** / **`RealmsImagePicker`** (`realms-image-picker.tsx`); bank APIs `POST /api/images` (+ replace/delete/usage); helpers `lib/realms-images.ts` (ADR-0003 / TASK-495–498). Legacy `CodexArtUploadField` + `/api/upload/codex-art` removed. |
| **Realms Image Library (bank)** | `realms_images` + `realms_image_categories`; `GET/POST /api/images`, `GET/PATCH/DELETE /api/images/[id]`, `POST …/replace`, `GET …/usage` (`ADR-0003`, TASK-492). Admin UI: `/admin/images` (TASK-493). Schema: `SUPABASE_SCHEMA.md` §2.5a; SQL `sql/realms-image-library.sql` |
| **Entity card art — product/schema** | REALMS §5.0.3 + `guide/03-entity-card-art.md` + `SUPABASE_SCHEMA.md` §2.5a |
| Hub list row (Encounters/Crafting) | `HubListRow` |
| Sortable column headers | `ListHeader` — data columns asc/desc by default; only spacer/action columns `sortable: false` (TASK-488). One `MobileSortMenu` for all three layout branches; active-sort chevron `text-primary-link-fg` (TASK-792) |
| Selection modal (add/pick from library) | `UnifiedSelectionModal` facade ~344 LOC (**TASK-618**) + co-located `unified-selection-modal-{types,helpers,toolbar,list,footer,leave-prompt}` (default `flexLayout` + `fullScreenOnMobile`; list-first: Search + Filters toolbar; `scopeExtra` always-visible mode tabs; `headerExtra`/`filterContent` collapsed in `FilterSection` compact; Cancel/Add Selected via Modal `footer` flush under list (`pb-0`); optional one-line `description` or omit; leave-with-selection **Add selected?** / **Load selected?** prompt — TASK-574; `maxSelections` soft limit when max ≠ 1; `maxSelections={1}` replaces selection; optional `nextSelectedIds` remaps add-path (innate energy last-in swap — TASK-727); `confirmLabel` / optional `primaryActions` / `footerExtra`). Sheet add proficiency: `AddProficiencyModal` (thin USM wrapper; option levels in `footerExtra` — TASK-567). Admin species trait Add: USM in `admin-species-edit-modal.tsx` (TASK-572/619). |
| AdminTraits choice-option picker | Inline **editor chrome** inside trait edit Modal (`admin-trait-edit-modal.tsx` — `useModalListState` + ListHeader + selectable GLR; TASK-572/619). Not an add-X USM — nesting a selection modal inside the edit Modal is wrong; keep search/sort/GLR grammar. |
| Encounter / session participant picker | **`AddCombatantModal`** (`add-combatant-modal.tsx`) — intentional non-USM shell (TASK-571). Creature Library + Campaign Characters drill-down; qty + combatant type + initiative/encounter payload. **Extend this** for combat/skill encounters (mixed reuses those views), VTT, downtime, and future session-play add-participant flows — do not fork and do not migrate onto USM. |
| Mixed-species dual picker (Advanced + sheet Edit Species + Guided Species L2 + Guided Ancestry mixed overview change) | **`MixedSpeciesModal`** (`character-creator/MixedSpeciesModal.tsx`) — intentional non-USM shell (TASK-605/641/720/714). Dual ordered species picks; source chrome is `SegmentedControl` typed with shared `SourceFilterValue` (species labels **Public species** / **My species** kept on purpose). Call sites: Advanced `species-step`, sheet `edit-species-modal`, Guided `species-step` L2 card, Guided Ancestry mixed overview **Change species**. **Do not** migrate onto USM or fork a parallel dual-select grammar. |
| + → ✓ selection button | `SelectionToggle`; equipped toggle: `EquipToggle`; innate: `InnateToggle` |
| Source scope All / Realms / My Library | `SourceFilter` (on `SegmentedControl`); catalog merge `mergeLibraryBySource` in `lib/library/source-scope.ts` (`LibrarySourceScope`; `SourceFilterValue` is an alias). Guided catalogs pass keep-selected draft ids; creature pickers do not (TASK-705 / TASK-709 / **TASK-712**) |
| 2–N pill toggle | `SegmentedControl` — hugs pills (`inline-flex`) unless `equalWidth` (TASK-720). Default size = SourceFilter chrome; `size="compact"` for dense sheet toolbars (All/Proficient — TASK-778) |
| Section header with + / collapse | `SectionHeader` (default `size="md"` / `text-sm`; Library list subsections pass `lg` / `text-base`; size on title so collapse cannot shrink it; `collapsible` + inline chevron beside title; `+` far right; collapsible rows use modest `pb-1.5` + coarse-only 44px); `useLibrarySectionCollapse`; `LibraryCollapsibleSection`; entity list sections `collapsible` prop; cost badge: `SectionCostBadge` |
| Dice roll button | `RollButton` |
| Roll provider / log (sheet, encounters, campaigns, creatures) | `@/components/rolls` (`RollProvider`, `RollLog`, `useRolls` / `useRollsOptional`); die helpers `lib/rolls/die.ts` (`rollDie`, `DIE_IMAGES`) — not under character-sheet |
| +/- steppers | `ValueStepper`, `DecrementButton`, `IncrementButton` share one `StepperGlyphButton` (pointer-capture + hold-repeat — TASK-792 / ADR-0002 / TASK-487); quantities: `QuantitySelector` (wraps ValueStepper), `QuantityBadge`; `UnifiedSelectionModal` `showQuantity` = in-row quantity-first |
| Sheet Edit vs Temp Modifier (exclusive) | ADR-0006 (Amended TASK-782): sheet-level play / edit / temp — toolbar pencil vs `SlidersHorizontal` (mutually exclusive; do not nest Temp under Edit). **Per-section open/close:** Edit uses `EditSectionToggle` (blue / green remaining / red over-budget); Temp uses `TempModifierToggle` (blue none / gold + / danger −). Header LargeStatBlocks + `Terminal: X` each have their own Temp sliders in Temp mode (not all steppers open at once). Persist `character.tempModifiers`; helpers `lib/character/temp-modifiers.ts` (`applyTempModifier`, `tempModifierValueClass`, `tempModifierTintFromDelta`, `sectionTempModifierTint`, `patchTempModifiers`, `withAbilitiesForResourceMaxima`, `hasAnyTempModifiers`). Library / Archetype keep `EditSectionToggle` in Edit only. |
| Character schema save/load normalization | `lib/character/schema-normalize.ts` (TASK-663): canonical `defenseVals`, `mart_prof`/`pow_prof`, `archetype.type`; dual-read legacy on load; strip on save — wired in `cleanForSave`, `prepareCharacterForSave`, character API routes. Dirty-key PATCH helpers: `lib/character/dirty-patch.ts` (ADR-0013 / TASK-741). Sheet postgres realtime: `mergeSheetRealtimePayload` (`lib/character/realtime-merge.ts`, TASK-747) overlays the remote blob via `mergeRemotePreservingDirty` (HP/EN/AP skipped); local dirty keys kept; adopted keys update the autosave baseline. |
| Point allocation display | `PointStatus` (`inline` = creator tracker size); Guided + Advanced Loadout/powers/finalize budgets: `shared/loadout-budget-bar.tsx` → `LoadoutBudgetBar` (TASK-614 / TASK-706; `CreatorResourceBar` removed); powered/martial split: `PoweredMartialSlider`. **Health/Energy pool:** `creator/health-energy-allocator.tsx` → `HealthEnergyAllocator` (PointStatus remaining/total; card labels Health/Energy on `md+`, HP/EN inline/narrow — TASK-729). |
| Skill row / allocation | Advanced/creature: `SkillRow`, `SkillsAllocationPage`, `AddSkillModal`, `AddSubSkillModal`. Sheet Skills (`skills-section`): catalog-all Codex **base** skills + compact All/Proficient (`SegmentedControl` `size="compact"`) + **Sub-Skills** toggle; merge/filter helpers `lib/character/sheet-skills-display.ts`; spend − clears value→prof (subs remove); no Add Skill / per-row X (TASK-584). Table `variant`: play view (`isEditing` false) hides `(species)` / `sourceLabel` and species-dimmed prof dots; edit/creator keep them (TASK-485). Sheet spend/temp: `SkillRow` `editControlsPlacement="inline"` puts compact `ValueStepper` + bonus caption in the Bonus cell (no fifth column — TASK-800); creator/allocation keep `column`. Guided L1: `GuidedSkillsPanel` (base + sub rows, Ability chip or governing-Ability picker + Skill Bonus formula tip + `DefenseBonusesCard` — TASK-548 / TASK-790); Guided L2/L3 browse: `skills-step` + `GuidedLayerNav` → `AddSkillModal` (opt-in on custom chooser) → footer **Browse all Sub-Skills** → `AddSubSkillModal`; shared apply helpers `lib/game/skill-allocation-add.ts` |
| Ability score grid | `AbilityScoreGrid` (`ability-score-grid.tsx`): display/edit tiles; `powerAbility` / `martialAbility` / `secondaryAbility` pills; `resolveDistinctSecondaryAbility`; display always shows full ability names (2-col phone / 3-col tablet / 6-col desktop — TASK-566); path pills: **Primary** / **Secondary** (guided UX for archetype vs recommended secondary); hybrids keep **Power** / **Martial** (both Archetype Abilities); full aria-label + single-line truncate; every tile reserves the same inner pill clearance while reduced grid top padding clears the outer straddle, keeping highlighted and unhighlighted display/edit tiles aligned (TASK-452, TASK-455, TASK-544, TASK-545, TASK-760); roomier edit grid; ability names use `WordHelpTip` + `getAbilityHelp` (TASK-547); `density="compact"` + `onlyAbilities` for Path More details recommended subset (TASK-578) |
| Guided path Primary/Secondary abilities | `resolvePathAbilityLabels` (`lib/game/path-ability-labels.ts`) + `buildPathAbilityChipLabels` (`lib/guided-creator/path-ability-labels.ts`); chip copy `formatPathPrimaryAbilityLabel` / `formatPathSecondaryAbilityLabel` (`lib/constants/copy/path-ability-copy.ts`) — Primary Ability chips for each Archetype Ability (two on powered-martial); Secondary only for distinct recommended; SoT for Guided + Advanced path cards and More details (TASK-544/545) |
| Guided path select + L1↔L3 | `path-selection-draft.ts` — `buildPathSelectionDraftPatch`, `buildOpenGuidedPathEntryPatch`, `buildOpenCustomPathEntryPatch`, `buildCreatorResetDraftPatch`, `buildEnterCustomArchetypeLayerPatch` / `buildEnterPathLayerPatch`, `buildCustomArchetypeDraftPatch`, `isGuidedCustomArchetypeComplete` (TASK-588/638/640/693; vitest) |
| Custom chooser deep catalog landing | `creator-entry-mode.ts` (`prefersDeepCatalogEntry`, `canContinueGuidedAbilitiesStep`, `resolveGuidedRecommendedAbilitiesPatch`), `guided-deep-entry-landing.ts`, `use-guided-deep-entry-on-arrival.ts` — custom entry + no `archetypePathId` opens L2/browse/customize on first step arrival (except **Skills** = opt-in Browse all Skills; **Ancestry** always lands species overview via `landsOnFirstInnerScreen` → phase 0 — TASK-697; vitest on the intent predicate) |
| Guided Species L2 + mixed Ancestry | `species-step.tsx` (L2 all species + `MixedSpeciesModal` card + **Create Species** via `GuidedLayerNav` below grid); `guided-species-resolve.ts`, `species-selection-draft.ts` (single/mixed draft patches), `guided-mixed-species-overview.tsx` (parent `GuidedChoiceCard`s + **Change species** → same `MixedSpeciesModal`; no granted-trait list — choices-ahead only), `ancestry-pick-tasks.ts` (`buildMixedAncestryPickTasks`); Ancestry Continue gate `ancestry-pick-gate.ts`; save via `build-character.ts` (TASK-641; vitest ancestry-pick-tasks + build-character + species-selection-draft). Ancestry overview granted traits: `GuidedChoiceCard` `readOnly` (TASK-711); species More details catalogs stay `GuidedTraitOptionList`. Size picker: `SegmentedControl` hugs few options (TASK-720). |
| Guided layer nav button chrome | `guided-layer-nav.tsx` + `guided-nav-button-styles.ts` — below-content placement (one left / pair left-right); expand = outline hatch (`guidedNavExpandClassName`); collapse = outline; footer Continue = primary (TASK-640, TASK-695) |
| Advanced ancestry step | `character-creator/steps/ancestry-step.tsx` (facade) + `steps/ancestry/{use-ancestry-step-state,ancestry-step-checklist,ancestry-mixed-panel,ancestry-single-panel}.tsx` (**TASK-610**); pick order lib: `ancestry-pick-tasks.ts` (TASK-591) |
| Guided Continue one-screen advance | `nextGuidedSubStep` / `prevGuidedSubStep` / `landsOnFirstInnerScreen` (`lib/guided-creator/guided-substep-nav.ts`) — store footer nav + Ancestry/Loadout entry intent; never jumps to furthest (TASK-592; vitest sub-step index + intent predicate; UI landing human T059) |
| Guided funnel integrity | derived rail `substep-satisfaction.ts`; skill prune `skill-reconcile.ts`; ancestry gate `ancestry-pick-gate.ts`; Loadout signed remainder + save `clampSavedCurrency` (`lib/character-save.ts`; Guided `build-character` + Advanced `getCharacter`, TASK-739; PATCH `prepareCharacterForSave` when `currency` is present, TASK-749); path `resolveArchetypeProficiencyStart`. SoT: `AUDIT_REMEDIATION_2026-08.md` Wave 2 |
| Guided step footer | `GuidedStepFooter`: sticky Back/Continue only (layer hops use `GuidedLayerNav`); `completionHint` stacks above actions below `sm`, centered mid-bar on `sm+` (one mount; TASK-453) |
| Tab summary header section | `TabSummarySection`, `SummaryItem` (`labelAccessory` for InfoTippy), `SummaryRow` — solid theme fills (`bg-*-light` / `bg-surface-alt`, no gradients); sheet Inventory Currency + Armament Proficiency stack below `sm` (TASK-537); Armament tip via `armamentProficiencyHelp` (TASK-581) |
| Chip roles (descriptor vs expandable) | `DescriptorChip`, `ExpandableChip` (`@/components/ui`); entity-row inline size `CHIP_ENTITY_INLINE_SIZE` / `chip-size-tokens.ts` (TASK-699); `GridListChip` + `lib/chip/expandable-chip-props.ts`; `ChipData.kind` + `descriptorChipData()` in `lib/chip/chip-data-helpers.ts`; optional `ChipData.onSelect` / `disabled` / `current` for control chips (sheet feat rank, TASK-780); metadata builders in `lib/chip/list-row-metadata.ts`. **Energy cost header is `N EN`, never EP** (TASK-755; Your Hero / sheet / Codex / creature chips that pass `energyCost`) |
| Stable expand toggle (chips) | `ExpandableChip` + `ChipGroup` (`data-chip-group`); `applyFullRowExpandLayout` (`lib/chip/full-row-expand-layout.ts`) — grow into remaining row width; do not force `w-full` on wrap expand (TASK-445). Header **or** expanded body toggles; Options uses `data-expand-ignore` (TASK-539). |
| Feat tags (normalize + taxonomy) | `lib/codex/feat-tags.ts`, `lib/codex/feat-list.ts`; `sql/feat-tags-unification-phase*.sql` (phase 4 = live normalize chain) |
| Part/property chips | `ExpandableChip` + `expandableChipPropsFromPartData` (`lib/chip/expandable-chip-props.ts`); `PartData` in `lib/chip/part-data.ts` (re-exported from `@/components/shared`); `partChipsFromDisplay` in `lib/chip/part-chips-from-display.ts` |
| Part/property → PartData (library rows) | `lib/library/part-display.ts` — `characterPartsToPartData`, `itemPropertiesToPartData` |
| Part TP math (powers/techniques) | `lib/calculators/part-training-points.ts` — `computePartTrainingPointsRaw` + `computePartTrainingPoints` (per-part **floor** is the GAME_RULES rounding exception), `PartTpVariant` |
| Path loadout flatten | `lib/game/loadout-entries.ts` — `flattenLoadoutEntries` |
| Dedupe saved parts / entity refs | `lib/game/dedupe-saved-parts.ts` — `dedupeSavedParts`, `dedupeEntityRefs` (creators save, sync, calc, sheet chips) |
| Empowered technique part chips (nested power + technique) | `lib/library/empowered-technique-display.ts` — `buildEmpoweredPowerDocument`, `deriveEmpoweredTechniquePartChips`, `empoweredTechniquePartsSection`; wired by Library empowered rows + `buildEmpoweredPowerSelectableItem` (TASK-626) |
| Sheet trait assembly (no species/ancestry dupes) | `lib/character/collect-sheet-traits.ts` — `collectSheetTraits` (Feats tab). Sheet rows: `library-feat-rows` `mapTraitRows` — Ancestry / Characteristic / Flaw as expanded-only descriptor chips (TASK-779); species kind omitted. State-feat Archetype/Character stay collapsed `badges`. Play view: player `note` in `descriptionAfter` (no Customize / custom-name field); edit keeps Customize fields. Italic custom names use `pe-[0.35em]` so truncate does not clip the slant (TASK-783). |
| Ancestry selection (Advanced + sheet Edit Species) | `lib/ancestry/ancestry-selection.ts` — trait buckets, mixed size/skills, draft builders, continue gates, toggles; UI `character-creator/TraitSection.tsx` (picker); SpeciesModal read-only catalog = local `SpeciesCatalogTraitSection` (TASK-594); sheet Edit Species = `edit-species-modal.tsx` + `use-edit-species-modal.ts` + `edit-species-species-step.tsx` / `edit-species-ancestry-step.tsx` (TASK-666f); mixed skills via shared `MixedSpeciesSkillPicker` (TASK-670) |
| Archetype path/forge edit helpers | `lib/game/archetype-edit.ts` — `listPlayerVisiblePaths`, `groupPathsByCategory`, `redistributeProficiency`, `buildPathSwitchResult`, forge ability gate; used by sheet Edit Archetype, Advanced archetype-step, Guided path-step; UI `character-creator/AbilityPickButton.tsx` (TASK-594) |
| Path recommendation collectors + list filter (by archetype path) | `lib/game/archetype-path.ts` — `collectPathRecommendedIds(pathData, kind)` (union of level 1 + later `levels[]` + guidance groups; `remove*` excluded, `id:qty` collapsed) → `lib/game/path-recommendation-index.ts` (`buildPathRecommendationIndex`, `pathRecommendedEntityIds`, `pathNamesForEntity`, `pathIdsForArchetypeType`, `applyLivePathFilter`, optional multi-kind bags) → `hooks/use-path-recommendation-index.ts` / `usePathListFilter` (memoized off `['codex', 'archetypes']`; optional same-type auto-select) → shared `ArchetypePathFilter` (last control in the filter grid). Feats: `filterFeats(..., { pathRecommendedIds })` + `featPathChipNames` / `featFamilyIdsMatchingPath`. Skills / mixed equipment / official powers (powers+innatePowers) / techniques / armaments: same index, no second match rule (TASK-751–752 / **ADR-0014**). Creator L2/L3 + AddFeat/AddSkill/AddLibraryItemModal: same control (TASK-753; Empowered skipped). `PowerTechniqueFilters` / `ArmamentFilters` take optional `pathFilter`. Row chips use `GridListRow` `showBadgesInName`. |
| Entity list sections (powers/techniques/weapons/armor/etc.) | `*ListSection` from `entity-library-sections` facade (TASK-611: columns/types/rows + powers-techniques / inventory / feats modules); multi-section tabs use `useLibrarySectionCollapse` + `SectionHeader` collapsible (empty closed; + Add expands) |
| Species / trait catalogs | `DetailOptionList` + `traitToDetailOption` (deep-dive / species-modal catalogs). Selection picks: `GuidedChoiceCard`. Ancestry overview granted traits: `GuidedChoiceCard` `readOnly` (TASK-711). (`ItemCard` / `SpeciesTraitCard` deleted — `/debt` 2026-07-19.) |
| Damage dice/type split (list cells) | `splitDamageDiceAndType` from `@/lib/utils` (single helper; sheet + entity lists + quick armaments) |
| Item ability requirements | `deriveAbilityRequirementFromProperties` + `AbilityRequirement` (`lib/game/weapon-attack-ability.ts`) — enrichment, official library, guided equipment; attack ability uses `resolveWeaponRangeDisplay` for melee vs ranged (TASK-716) |
| Shared list filters | `@/components/shared/filters` (`ChipSelect`, `ArchetypePathFilter`, `FilterSection`, `PowerTechniqueFilters`, `ArmamentFilters`, `CharacterFilter`, `FilterInput` / `FilterNativeSelect`, …) — not `@/components/codex`. `ChipSelect` takes an optional per-option `group` for `<optgroup>` + `labelAccessory` (InfoTippy); `ArchetypePathFilter` composes it (union multi-select, no any/all toggle — ADR-0014). Filter number/text fields and native selects share `FILTER_CONTROL_CLASS` (`h-11` + `rounded-md`; TASK-725). Library + Codex feats/skills/equipment character id persistence: `lib/library/character-filter-persistence.ts`. `CharacterFilter` is a collapsible subsection (default collapsed; InfoTippy immediately after the title — TASK-781). Codex skills: known / not known / base-owned via `lib/codex/skill-list.ts` (TASK-722). Codex/Admin mixed equipment columns + chips: `lib/codex/equipment-list.ts` (TASK-723; `ArmamentFilters` `showEquipmentExtras`). Browse lists: FilterSection compact via ListSearchToolbar (TASK-721); `PowerTechniqueFilters` / `ArmamentFilters` are panel bodies only; optional `pathFilter` renders `ArchetypePathFilter` last (TASK-752). |
| Library add-to-character (+ row action) | `useAddToCharacterFromLibrary` (`hooks/use-add-to-character-from-library.tsx`) + `map-library-to-character.ts`; `LibraryAddToLibraryButton` / `LibraryAddToCharacterButton` (`shared/library-add-to-library-button.tsx`) + `LibraryRowActionSlot` (`shared/library-row-action-slot.tsx`); wired on Library + Realms power/technique **and** weapons/armor/shields lists when character filter selected (TASK-679 / TASK-680). Icons: **BookPlus** = add to My Library; **UserPlus** = add to character (TASK-731 — not a second Plus). Save goes through `useSaveCharacter` (`updatedAt` lock + 409 re-apply, TASK-746 / ADR-0013). |
| Creature stat block | `CreatureStatBlock` facade (TASK-611: types/helpers/section/display-data/panels) — Speed/Evasion via `calculateCreatureSpeed` / `calculateEvasion` (no size add; TASK-742); weapon Attack uses shared `getWeaponAttackBonusFromProperties` (`lib/game/weapon-attack-ability.ts`; same as sheet / quick armaments); level column/subline via `formatCreatureLevel*` (`lib/game/creature-level-display.ts`) |
| Creature library stat-block rows | `CreatureLibraryStatBlockRow` (single row) + `CreatureLibraryStatBlockRows` (My Library list) — both use `mapLibraryCreatureToStatBlockData`; Realms browse renders rows via `OfficialEntityList.renderRow` |
| Filters | `ChipSelect`, `ArchetypePathFilter`, `TagFilter`, `SelectFilter`, `AbilityRequirementFilter`, `CharacterFilter`, `SourceFilter`, `FilterInput` (`shared/filters`) |
| List states | `ListEmptyState`, `LoadingState`, `ErrorDisplay` |
| Search box | `SearchInput` |
| List browse search row (+ Filters + trailing) | **`ListSearchToolbar`** (ADR-0011 / TASK-721) — `CodexBrowseListShell`, `UserLibraryEntityTabShell`, `OfficialEntityList`. When `filters` is set, composes FilterSection compact `toolbarStart` (Search + Filters one row; `trailing` after Filters). |
| Official library entity lists (browse + admin) | `OfficialEntityList` (+ optional `filters` slot) + thin wrappers: `OfficialPowerList` / `OfficialTechniqueList` / `OfficialItemList` (`armamentKind`: weapon \| armor \| shield; config in `lib/library/official-item-list.ts`; `ArmamentFilters` + character add-to-sheet TASK-680) / **`OfficialCreatureList`** (library variant = full `CreatureStatBlock` rows; admin = compact grid) / **`OfficialEnhancedList`** (TASK-575; helpers in `lib/library/official-*-list.ts`). Optional `searchTrailing` for admin Create beside search. Power/technique: `PowerTechniqueFilters` + `derivePartCategories` / Damage category for damaging powers (TASK-673) + `power-technique-character-context` + character/TP caps (TASK-676); **USM add-power/technique/item** reuses those panels as the FilterSection body with optional `pathFilter` (TASK-675 / TASK-753; Empowered skipped). |
| Admin Codex + Codex browse list chrome | **`CodexBrowseListShell`** (ADR-0005) — SectionHeader (admin) + `ListSearchToolbar` (Search + Filters on one row when `filters` is set; TASK-721) + ListHeader + loading/empty/rows. Row children window-virtualize (`@tanstack/react-virtual`) past 40 rows; shorter lists and a single grouped child (Codex Archetypes) mount as before (TASK-775). Not for Official* grids or **Admin** Archetypes path rows. Admin Images bank list uses the same shell. |
| Confirm/delete/login modals | `ConfirmActionModal`, `DeleteConfirmModal`, `LoginPromptModal` (guest Save/Load; Log In / Create Account / Continue Without Saving in Modal `footer`; TASK-802) |
| Standalone creator page shell (auth/load/save) | `CreatorPageShell` (+ `CreatorLayout`, `CreatorSaveToolbar`, `CollapsibleSection`) from `@/components/creator`. Expanded `CollapsibleSection` headers keep original title/padding (`p-4`, `font-bold` / global `h2` `text-title`) and drop the reserved empty summary line (TASK-764). Optional `toolbarHelp` / `CostStat.help` / `costHelp` for InfoTippy. |
| Image upload + crop | `ImageUploadModal` |
| Creator portrait (Advanced finalize + Guided reveal) | `CreatorPortraitUpload` — wrappers `PortraitUpload` / `GuidedPortraitUpload`; draft compress + Storage via `lib/portrait.ts`; **display matte** `bg-image-matte` only (crops preserve alpha PNG/WebP, no theme bake) |
| Realms Image Library picker | `RealmsImagePicker` — bank browse/select; admin upload-into-bank (`resolveRealmsImagePickerCategories` in `lib/realms-images.ts`) |
| Theme switch | `ThemeToggle` |
| Help tooltips | `InfoTippy` (`tone` API: `info` / `tp` / `current` — TASK-707; layout-neutral hit overlay TASK-725) / `WordHelpTip` + `public/tooltip-text.tsx` — see `guide/04-floating-ui-tooltips.md` |

> UI primitives (Modal, Button, Chip, **DescriptorChip**, **ExpandableChip**, PageContainer, PageHeader, TabNavigation, SearchInput, **TableScroll**) live in `@/components/ui`. Modal sizes: `full` for high-complexity editors (admin codex add/edit use `full` + `fullScreenOnMobile`; see DESIGN_SYSTEM). After close, Modal holds an invisible `z-overlay` sink for 200ms (TASK-802) so dismiss cannot click-through to Create/Save.

## Game logic / calculators (`src/lib/`)

| Need | File |
|------|------|
| Health/skill/derived calculations (current source) | `lib/game/calculations.ts` (`calculateMaxEnergyForArchetype` / `resolveEnergyArchetypeAbility`, `abilityDefenseBonusesFromAbilities` for allocation cards, `calculateCreatureSpeed` = player Speed with **no size add**; Energy uses the higher of pow/mart on sheet, Advanced, and Guided) |
| Game constants / formulas | `lib/game/constants.ts` (`CHARACTER_STARTING_CURRENCY` — Guided + Legacy; do not import from the Legacy store — TASK-791), `lib/character/appearance-age.ts` (`parseAgeFromAppearance` / `mergeAgeIntoAppearance`; Legacy identity fields + Guided save), `lib/game/formulas.ts` (`resolveArchetypeProficiencyStart` — path `*_prof_start` else type defaults; Guided save + Advanced path select; `calculateMaxArchetypeFeats` delegates to `calculateArchetypeProgression` so Powered-Martial milestone feat picks count — TASK-742); Levels-by-Rarity: `LEVELS_BY_RARITY` + `maxRarityForCharacterLevel` / `rarityAtOrBelowMax` in `lib/game/creator-constants.ts` |
| Skill allocation | `lib/game/skill-allocation.ts` (`canIncreaseDefense` caps **skill-point** defense at level; ability bonus unrestricted) |
| Feat requirement checks | `lib/game/feat-requirements.ts` — `checkFeatRequirements`: hard `lvl_req` else character level ≥ 2 × feat level (feat rank 1 with no `lvl_req` stays legal at L1); used by creators, sheet, codex filter, L1 create legality (TASK-742) |
| Leveled feat families | `lib/leveled-feats.ts` — family grouping, `formatFeatName` (`(Level N)`), Codex other-rank chips (`buildFeatLevelChips`), sheet play current-marked chips (`includeCurrent`), sheet edit rank via the same builder (`select` → `ChipData.onSelect`, TASK-780) |
| Creator skill-save rows (Guided + Advanced) | `lib/creator/build-creator-skills.ts` (`buildCreatorSkillSaveRows` — preserves proficient skill_val 0 before `cleanForSave`; Guided `build-character` + Advanced finalize) |
| Read-only sheet assemble (campaign RM view) | `character-sheet/read-only-sheet.ts` (`buildReadOnlySheetContextValue` / `buildReadOnlyLibraryHandlers`) + `useCharacterSheetDerived` + `CharacterSheetBody`. Catalog args come from `sheetCatalogFromEnrichment` (`lib/character-view-enrichment.ts`) on the GET payload (TASK-773), not viewer `useUser*` / `useOfficialLibrary` / `useCodex*` |
| Equipment equipped rules (single armor, create auto-equip) | `lib/game/equipment-equipped.ts` |
| Armor Damage Reduction (canonical) | `lib/game/resolve-armor-damage-reduction.ts` → **`resolveArmorDamageReduction`** (TASK-644) — sheet equip, official list, enrichment, guided catalog; `itemDamageReduction` in `equipment-equipped.ts` is a deprecated alias |
| Sheet header DR / Critical Range vitals | `sheet-header.tsx` facade + `sheet-large-stat-block.tsx` (`LargeStatBlock`) + `sheet-header-identity` / `sheet-header-resources` (TASK-598 / **TASK-788**); play/edit show DR only when equipped armor (or a temp) changes DR; Critical Range only when armor Critical Range +1 / Increase (1 + op_1_lvl) or a crit temp applies. **Temp mode always shows both cards** so a temp can be added. Formula: `calculateCriticalRange` = Evasion + `COMBAT.criticalHitThreshold` (10) + increase (`getEquippedArmorQuickRef`; archetype `QuickArmorTable` uses the same helper) |
| Archetype path / progression | `lib/game/archetype-path.ts` (incl. `pathHasPlayerVisibleLevel1`, `pathHiddenFromPlayerPicker`, `innatePowers`; `PathGuidanceGroup.audience` + `filterFeatGuidanceGroups` / `resolvePathGuidanceAudience` — TASK-514/ADR-0004; shared parsers `parseOptionalJsonField` / `parseIdQuantityStrings` / `serializeIdQuantityStrings` / `parseRecommendedAbilities` — TASK-476/570), `lib/game/archetype-display.ts`, `lib/game/archetype-edit.ts` (sheet + Advanced path list/group + path-switch save — TASK-594), `lib/game/innate-eligibility.ts`, `lib/game/path-validation.ts` (Appendix G innate + Level 1 skills warn-cap — TASK-515), `lib/constants/creator-layer-governance.ts` (`LAYER1_GOVERNANCE.maxPathRecommendedBaseSkills` = 3), `components/character-sheet/path-level-guidance.tsx` (`indexDisplayNamesByNormalizedIds` — TASK-732), `components/character-sheet/archetype-path-identity.tsx`, `components/character-sheet/edit-archetype-modal.tsx`, `app/(main)/codex/CodexArchetypesTab.tsx`, `app/(main)/admin/codex/AdminArchetypesTab.tsx` + `admin-archetype-path-form.ts` + `admin-archetype-path-rows.tsx` + `admin-archetype-editor.tsx` (facade ~98 LOC; TASK-609 sections: `admin-archetype-editor-{config,meta,level1,guided,progression}.tsx`) + `use-admin-archetype-workspace.ts` (facade ~242 LOC; TASK-617: `admin-archetype-workspace-{open,save,mutators,unknown-selections}.ts`) + `use-admin-archetype-selection-options.ts` (TASK-381 Phase 6a–6c; feat guidance groups character vs archetype; L1 skills ≤3 base + warn; armaments weapons/shields vs armor UI — TASK-514–516; no path-recommended species — TASK-517; recommended-ability steppers + loadout — TASK-404; Advanced Path JSON escape hatch for rare fields; selected feats expandable GridListRows + full-width qty rows — TASK-534), creator `skills-step` / `feats-step` apply actions; species L1 = `is_starter` only |
| Admin codex spreadsheet | `CodexSpreadsheetView.tsx` (facade ~115 LOC; **TASK-617**) + `use-codex-spreadsheet.ts` + `codex-spreadsheet-{config,helpers,toolbar,table}.tsx` — raw tab spreadsheet edit/find-replace/save. Collection allowlist / id retirement / delete probes: `lib/codex/{collections,id-allocation,references}.ts` |
| Admin codex species editor | `AdminSpeciesTab.tsx` (facade ~321 LOC; **TASK-619**) + `admin-species-form.ts` + `admin-species-edit-modal.tsx` — CodexBrowseListShell list + full modal editor |
| Admin codex properties editor | `AdminPropertiesTab.tsx` (facade ~357 LOC; **TASK-619**) + `admin-property-form.ts` + `admin-property-edit-modal.tsx` |
| Admin codex traits editor | `AdminTraitsTab.tsx` (facade ~296 LOC; **TASK-619**) + `admin-trait-form.ts` + `admin-trait-edit-modal.tsx` (+ nested create-trait modal) |
| Admin codex feats editor | `AdminFeatsTab.tsx` (facade ~368 LOC; TASK-609) + `admin-feat-form.ts` + `admin-feat-edit-modal.tsx` (facade ~265 LOC; **TASK-619**) + `admin-feat-edit-modal-fields.tsx` |
| Admin codex parts editor | `AdminPartsTab.tsx` (facade ~390 LOC; TASK-609) + `admin-part-form.ts` + `admin-part-edit-modal.tsx` (facade ~305 LOC; **TASK-619**) + `admin-part-edit-modal-options.tsx` |
| Admin core rules editor | `admin/core-rules/page.tsx` (facade ~142 LOC; TASK-609) + `core-rules-category-editor.tsx` facade (**TASK-666g**) + `core-rules-{tabs,field-editors,progression-preview,damage-types,progression,combat,archetypes,ability-skills,conditions,sizes,rarities,recovery-experience,armament,crafting-rules}-editor(s).tsx` |
| Crafting / encounter helpers | `lib/game/crafting-utils.ts`, `lib/game/encounter-utils.ts` |
| Creature level display (user-facing fractions) | `lib/game/creature-level-display.ts` — `formatCreatureLevel`, `formatCreatureLevelLabel`, `formatCreatureLevelShort`, `parseCreatureLevelSortValue` (list sort), `CREATURE_LEVEL_SELECT_OPTIONS`; export `@/lib/game` |
| Power / technique / item / empowered calc | `lib/calculators/*-calc.ts`, `mechanic-builder.ts` (`calculateDamageOptionLevel`), `empowered-overlap-parts.ts` (cheaper-EN pick — TASK-683). Empowered technique EN uses `dedupeSavedParts` on percentage parts (TASK-742). Item rarity from IP; currency clamped to that band's `currencyMax` (`calculateCurrencyCostAndRarity`). Item damage: `deriveItemDisplay` delegates to `formatDamageDisplay` (TASK-715). Item/weapon range display: `resolveWeaponRangeDisplay` (TASK-701 / **TASK-716**); `formatRange` is a properties-only internal derive in `item-calc.ts` (not barrel-exported) |
| Data enrichment (minimal stored → full display) | `lib/data-enrichment.ts` facade → `lib/data-enrichment/*` (TASK-611: enrich powers/techniques/items/character + clean-for-save). Read-only other-user / RM catalog rows: `lib/character-view-enrichment.ts` (client collector + `sheetCatalogFromEnrichment`) + `lib/character-view-enrichment-server.ts` (referenced fetch; same P0-1 gate as `getOwnerLibraryForView`). Codex row map: `lib/codex/row-map.ts` (shared with GET `/api/codex`; TASK-777) |
| Character sheet Library tab visibility defaults | `lib/character-library-tab-visibility.ts` (`defaultLibraryTabVisibilityForArchetype` → persisted `libraryTabVisibility`; power/martial-only create hides opposite tab) |
| Library columnar mapping & sync | `lib/library-columnar.ts` (`columnarViewSelect` for official/user view fetches), `lib/library-sync.ts`, **`lib/library-selectable-builders.ts`** (shared add+load SelectableItem pipeline; `libraryItemToPowerDocument` / `libraryItemToTechniqueDocument` + `buildPowerTechniqueBudgetDisplay` / `buildPowerTechniqueFilterableRow` for guided + Official list derive parity — TASK-691 / TASK-675 / **TASK-708**; vitest covers DEV-V-016-T001/T002/T003 column contracts) |
| Load from library (creators) | `LoadFromLibraryModal` + `useLoadModalLibrary`; species/creature rows: `@/lib/library/creator-load-selectables` |
| Tooltips (defaults + interpolation) | `lib/tooltips/` — **`lib/tooltips/README.md`** (PR #14 onboarding; legacy DB key map in that README) |
| Roles / quotas / limits | `lib/role-policy.ts`, `lib/role-limits.ts`, `lib/role-quota-messages.ts`, `lib/admin.ts` |
| API client / validation / rate limit | `lib/api-client.ts` (`apiFetch`, `apiUpload`, `ApiError` / `isConflictError`, `getErrorMessage`, `logClientError`), `lib/api-validation.ts`, `lib/validation/schemas.ts` (auth emails trim+lowercase), `lib/rate-limit.ts` (Upstash Redis / Vercel KV when env set — TASK-645; **prod Redis live 2026-08-13** via `KV_REST_API_*` on Vercel, TASK-669) — client error convention: `ARCHITECTURE.md` § Client error handling |
| Level-1 create legality (server backstop) | `lib/game/character-legality.ts` — `findLevel1LegalityViolations` / `shouldCheckLevel1Legality`, called by `POST /api/characters` (TASK-738; vitest). **Bounds only** for spend/counts: over-budget is refused, under-filled is not, and each budget is taken at the more permissive of `core_rules` override and code default so it can never 400 a build a creator allowed. Skill spend is a deliberate lower bound (species/path proficiencies are free and invisible to the server). **Feat requirements are not a budget**: catalog feats that fail `checkFeatRequirements` are refused; ids not in `codex_feats` are skipped. Catalog fetch selects live `codex_skills.base_skill` (TEXT) and `mapCodexBaseSkillToId` maps it to app `base_skill_id` (same helper as GET `/api/codex`; TASK-754). Create 500s use `GUIDED_CREATOR_COPY.steps.reveal.saveFailed` via `apiErrorResponse` (no Postgres `message`/`hint`/`code`). Guided/Legacy create toasts use `formatCharacterCreateFailureMessage`: 400 legality keeps the violation list; 500s do not append `saveRetryHint`; My Characters / duplicate copy only when the POST may already have a row (network/abort) |
| `core_rules` on the server | `lib/core-rules-server.ts` → `fetchCoreRules(supabase)`; one authority, shared by `/api/codex` and the create legality check (TASK-738). Client equivalent: `useGameRules` |
| Auth error copy (login/register/reset/update-email/update-password/delete-account) | `lib/auth-errors.ts` (`getAuthErrorMessage`) — do not map every message containing “email” to invalid address; my-account uses `update-email` / `update-password` / `delete-account` |
| Supabase clients (server/client/middleware) | `lib/supabase/*` |
| Generic utils (cn, string, number, object, motion, duration display) | `lib/utils/*` — list column labels: `formatColumnKeyLabel()` in `string.ts`; item damage: `formatDamageDisplay()` (string / object / array — TASK-715); motion timing: `MOTION_DURATION_SLOW_MS` in `motion.ts`; duration layers in `duration.ts`: structured `formatDurationFromTypeAndValue` / `formatDurationWithModifiers`, any-shape `formatDurationDisplay`, compact list `formatDurationCompact` (TASK-477); id lookup: `normalizeId` / `rowMatchesNormalizedId` / `findByNormalizedId` / `indexByNormalizedIds` / `indexDisplayNamesByNormalizedIds` / `resolveNormalizedRefLabel` in `normalize-id.ts` (id + `docId`; path `id:qty` names — TASK-732) |
| Stable empty fallbacks for hook deps | `lib/empty.ts` (`EMPTY_STRING_ARRAY`, `EMPTY_NUMBER_RECORD`, `EMPTY_STRING_RECORD`, `EMPTY_GUIDANCE_GROUPS`) — never mutate |

## Services (`src/services/`, import from `@/services`)

| Need | Service |
|------|---------|
| Character persistence | `character-service` |
| Library entity types (official + user) | `src/types/library.ts` (`LibraryPower`, `LibraryTechnique`, `LibraryItem`, …); hooks re-export |
| Official / user enhanced items | `src/types/crafting.ts` (`OfficialEnhancedItem`, `OfficialEnhancedItemPayload`, `CreateOfficialEnhancedItemInput`); `useEnhancedItems` |
| Library fetch/save (official + user) | `library-service` (use `fetchOfficialLibrary`, not removed `fetch*Public*` aliases) |

## Stores (`src/stores/`)

Zustand stores live in `src/stores/`. Check there before adding client-side global state (auth state is via `useAuthStore`).
