/**
 * Campaign Constants
 * ===================
 * Shared limits and configuration for campaigns.
 */

/** Maximum total characters in a campaign (owner + all players) */
export const MAX_CAMPAIGN_CHARACTERS = 10;

/** Maximum characters the Realm Master can add of their own */
export const OWNER_MAX_CHARACTERS = 5;

/** Maximum rolls stored in campaign roll log */
export const MAX_CAMPAIGN_ROLLS = 50;
