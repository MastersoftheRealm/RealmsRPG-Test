/**
 * Motion timing constants — keep in sync with CSS tokens in globals.css:
 * --duration-fast (150ms), --duration-base (200ms), --duration-slow (300ms)
 */

/** Exit animation duration for toast dismiss (matches --duration-slow). */
export const MOTION_DURATION_SLOW_MS = 300;
