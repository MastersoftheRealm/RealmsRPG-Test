/** Path card / overview ability chip labels (Guided + Advanced creators). */

export function formatPathPrimaryAbilityLabel(formattedAbility: string): string {
  return `Primary Ability: ${formattedAbility}`;
}

export function formatPathSecondaryAbilityLabel(formattedAbility: string): string {
  return `Secondary Ability: ${formattedAbility}`;
}
