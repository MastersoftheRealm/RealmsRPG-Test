# Supabase (from Supabase Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Database (from Supabase Dashboard → Settings → Database → Connection string)
# Session Pooler (port 6543) for serverless — append ?pgbouncer=true (required for Prisma + Supabase)
DATABASE_URL=
# Direct connection (port 5432) for migrations — no pgbouncer param
DIRECT_URL=

# Optional: Admin UIDs (comma-separated) for /admin access
ADMIN_UIDS=
